# 全量方法论（新族 / 破案 / 复验争议时读）

> 适用场景：P1 采到未知模式（新族）、注入故障需要根因定位（破案）、或对既有规格有争议需重判。确认性探测（已知族）不用读本文件——走 SKILL.md 快路径即可。

## 出处

- 完整实验设计与判据：MAM 项目 `docs/superpowers/plans/2026-09-18-phase2-m6r-windows-probe.md`
- 实测报告与定案：`research/refs/phase2-消息注入/2026-09-18-M6R-windows注入探测报告-v2.md` + 独立评审同目录
- 语义与源码证据：`research/refs/phase2-消息注入/2026-09-18-Windows控制台写入语义与消费墙复核.md`
（非 MAM 环境缺这些文件时，按下方清单自足执行）

## 实验全集（E1–E13，破案按需选取；确认性探测 = 快路径 P0–P7 的超集）

| # | 实验 | 判据要点 |
|---|---|---|
| E1 | 输入模式四态采样（idle/读武装/busy/审批 UI） | LINE_INPUT=1 → cooked-read 行缓冲假说；VT_INPUT 位 → A/B 族判别 |
| E2 | 错误码实采（固定长度逐次注入） | 首个失败的真实错误码；122 出现于 WriteConsoleInput 本身 = 反常，查自己 FFI/编码转换 |
| E3 | 缓冲占用率曲线（50ms 采样并排） | 写失败+占用高=积压；写成功+占用 0+框不涨=边读边丢；先涨后骤降=延迟消费或 Flush |
| E4 | 消费感知节流（1 字符→等占用归零→200ms→下一个） | 全量到达=无墙；永久停摆=真约束（记录停摆点/恢复时刻） |
| E5 | 参数组合验证（80/50ms/150ms × 长度三档 × 双宿主 × ≥5 取样） | 参数定案的统计基础 |
| E6 | bracketed-paste（ESC 用 [char]27；先 Peek 验证入缓冲） | 三家行为可能分化（A 族可用 / B 族有害） |
| E7 | 退格对照（墙态中交替文本/退格） | 退格通+文本堵 → 行编辑器侧归因 |
| E8 | 无人读控制台直证（一次写 200 记录） | 全量成功 = 「缓冲容量墙」证伪 |
| E9 | busy 全程观测（1s 探针+占用+5s 截图覆盖整 turn） | busy 期排队/忽略/丢弃三态判定 |
| E10 | 文本与回车分离（先文本→截图→5s→单独回车） | 区分「文本被截」vs「回车丢失」；回车被吞=写入确认必要性实证 |
| E11 | 成对 vs 仅 keydown 对照 | 双写判定（keyup 过滤与否） |
| E12 | VK vs VT 方向键对照 + 非零扫描码对照 | 事件形态规格定案；**每格都要留记录+截图，没测不许写生效** |
| E13 | 报告 + 参数定案表 + 独立评审 | 结论-证据一致 / 判据执行 / 纪律合规 / 控制变量 / 无凭空值 |

## 已定案的宏观事实（破案时先对照，别重复劳动）

1. 控制台输入缓冲无界（官方文档 + conhost 两代源码 + ReactOS + E8 直证）——「缓冲满」不存在。
2. WriteConsoleInput 正常路径无 122；错误码必在客户端侧找（FFI 布局/参数错位/编码转换）。
3. M6「消费墙」三结论（首读预算 13~43 字符 / 122=缓冲满 / 再武装 30s~10min）均被推翻；旧「停摆」观测的真身候选 = 宿主文本选择模式冻结（写入成功、TUI 读不到、PostMessage 可解冻）。
4. 真实约束 = TUI 事件形态过滤（B 族丢 vk=0 控制字符、吃裸 ESC）+ busy 语义差异。

## 破案流程建议

1. 先复现故障现象（保持条件与报告者一致）；
2. E3 占用率曲线定故障类别（写入层/缓冲层/消费层）；
3. 按类别选实验：写入层→E2；缓冲层→E8；消费层→E1/E7/E12；
4. 版本升级后复验：只跑 P3/P4/P5 + 该族关键分支项（15 分钟级）。

# 安全策略

## 支持的版本
| 版本 | 支持状态 |
|------|---------|
| 0.2.x | 支持 |
| < 0.2 | 不支持 |

## 报告漏洞
请不要公开提交 Issue。使用 GitHub Security Advisories 或发送邮件至维护者。

## 安全原则
- 所有监控在本地完成，不向外传输数据
- 敏感路径（SSH 密钥、.env、凭据）不得被读取或暴露
- 安全扫描检查 skill 文件路径防止路径穿越

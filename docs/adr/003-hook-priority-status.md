# ADR 003: Hook 优先 + notify + 轮询三重策略

**状态**: 已接受 | **日期**: 2026-07-05

## 背景
AI 工具会话状态变更需要及时捕获。不同工具能力不同：有 Hook 的工具可事件驱动，无 Hook 的工具只能进程扫描。

## 决策
三重策略：Hook 事件（最高优先级）-> notify 文件监听（变化时立即触发）-> 30s 定时轮询（兜底）。

## 后果
- 正面：状态变更延迟从 3s 降至 <1s
- 负面：三重策略增加复杂度

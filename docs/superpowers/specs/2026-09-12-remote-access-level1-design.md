# 一期设计说明书 · 第 8 工具监控 + 远程接入与移动看板（只读）v5

> 上位文档：`docs/MASTER-PLAN.md`（项目宪法 v1.0，D1–D15）。冲突时以宪法为准。
> 版本：2026-09-12 v1 → 2026-09-13 v2（并入 M0）→ v3（产品语言重写）→ 2026-09-14 v4 定稿 → **2026-09-14 v5（M1 评审裁决落档）：①未读语义对齐实现——红/blocked 不挂未读角标，绿卡由通用未读池标记（裁决 D3）②Skill 符号链接管理纳入一期（用户裁决 D4，宪法 D14 已补记）**。
> 实现细节（模块/函数/接口/算法）一律不在本文档，统一入口：`research/refs/phase1-接入层/一期实现要点-技术备忘.md`。
> 状态：待用户最终确认后进入实施计划。

---

## 1. 目标与范围

一期交付两件事，按依赖顺序：

1. **dsh（DeepSeek harness）成为第 8 个受监控工具**：与现有七个工具同等待遇出现在 MAM 看板——状态、内容、预览、跳转（宪法 D14，只读）。
2. **远程接入与移动看板**：手机/外部浏览器经安全通道接入本机 MAM，看到八工具看板与会话内容，状态变化 2 秒级提醒；交付 APK 壳（M5）。

**一期不做（边界，非选项）**：任何写操作（发消息/切模型/审批应答）；多用户账号体系；端到端加密；HMS 原生推送；iOS 原生应用；dsh 插件开发。**例外（用户 2026-09-14 裁决）**：dsh 的 Skill 符号链接管理（补链/启停，写 `~/.dsh/skills`）纳入一期——属 MAM 资源管理既有能力与其他工具同等待遇，不属消息类写通道（后者仍按宪法 D14 另评）；dsh 会话数据（sessions/storages）仍只读。

---

## 2. 第一部分：dsh 第 8 工具监控（P1–P5）

### P1 · dsh 会话出现在看板上

- **需求**：MAM 自动发现 dsh 进程与全部会话（含历史会话），生成与其他工具一致的会话卡片。
- **输入**：dsh 在本机留下的运行痕迹（进程、端口、会话索引、会话日志）。
- **输出与效果**：看板新增 dsh 卡片——会话标题、所属项目、三色状态、最后消息预览、运行时长（与其他工具同口径）；后台子任务会话不产生卡片。token/耗时用量为**远期增强**（数据已在 projcache 备妥，当前卡片模型无此字段，一期不做）。
- **边界**：只读取证；不安装任何 dsh 插件；不触碰其鉴权接口。

### P2 · 三色状态准确

- **需求**：dsh 的红/黄/绿与其他工具同义——红=需要用户（等批准/出错/已中断）、黄=运行中、绿=完成或空闲。
- **输入**：会话事件与索引中的回合边界、审批请求、结束原因。
- **输出与效果（一期行为定死）**：
  - 等待批准、运行失败 → **红**（红态颜色 + 桌面通知即注意信号，不另挂未读角标——与七工具实际行为一致，2026-09-14 裁决）；
  - 会话写入方已死（进程崩溃）→ **红 · 中断**；判定依据是"写入者是否存活"，与任务运行时长无关，长任务永不误判；
  - 运行中 → **黄**；刚完成 → **绿**，计未读；已读 → 绿 · 空闲；
  - 用户主动取消 → **绿 · 空闲**，不亮红、不计未读；
  - 回合被阻塞（blocked）→ **黄 · 等待中**（不挂未读角标，同上）；
  - 达到 token 上限（max-tokens）→ **绿 · 完成**，计未读。
  - 全部状态变化接入现有通知与提醒链路。
- **边界**：旧格式目录无存活信号，沿用同一"写入者存活"判定的降级路径（时长兜底，阈值固定 30 分钟）。

### P3 · 消息预览可信

- **需求**：卡片与通知中的"最后消息"必须反映真实对话，绝不混入系统注入内容。
- **输入**：会话消息流。
- **输出与效果**：预览只取真人输入与助手回复；系统指令、技能清单等注入内容一律过滤；助手纯工具调用帧显示「执行 <工具名>」。

### P4 · 一键跳转

- **需求**：点击 dsh 卡片回到 dsh 工作界面。
- **输入**：卡片点击；dsh 服务地址。
- **输出与效果**：dsh 页面已打开 → 连同浏览器标签一起聚焦；未打开 → 打开 dsh 首页且免重复登录；聚焦失败 → 卡片给出"请手动打开"提示。**不做会话级直达**（官方与全部已装插件均无此入口，已穷举验证，此为一期定案而非取舍）。

### P5 · 存量兼容与升级降级

- **需求**：旧格式历史会话照常可见；dsh 升级后 MAM 不崩、可感知地降级。
- **输入**：多代并存的会话文件格式。
- **输出与效果**：存量旧格式会话正常解析展示；遇到未识别格式/版本时跳过该会话，显示"格式待适配"降级卡，不影响其他会话与看板稳定性；已验证 dsh 版本登记研究库跟踪清单，升级后按版本门流程复测。

**第一部分验收（= 一期收尾验证项"DSH 作为第 8 工具的监测效果"）**：真实使用中依次制造等批准 / 运行中 / 完成 / 用户取消 / 强杀进程五种场景，看板状态全部正确、预览无注入内容、跳转可用、桌面通知链路无回归。强杀场景预期（2026-09-14 裁决 D6）：宿主被杀 → 卡片随宿主消失（与全工具统一语义）；"红·中断"的可达场景为杀持锁的无头进程（web 宿主存活）。

---

## 3. 第二部分：远程接入与移动看板（P6–P12）

### P6 · 远程开关与设备配对

- **需求**：远程访问默认关闭；开启后以**批准制**接入新设备——固定地址不是秘密，批准才是钥匙。一期交付两种配对方式（均已定案，非可选其一）：
  - **审批配对（默认）**：新设备打开接入地址 → "请求接入"页 → 桌面 MAM 弹通知并在配对面板出现待审批项（设备名/来源/浏览器）→ 用户批准，或让手机输入桌面显示的 4 位确认码（等效授权，单设备限试 3 次）；审批请求 5 分钟有效，过期自动失效。
  - **直通配对（设置开关，默认关闭）**：一次性二维码临时分享，刷新即失效，用于快速给同事看一眼。
- **输出与效果**：一次配对默认 180 天有效，期间同一浏览器免任何输入；设备花名册可查看在线状态、单独吊销、全部吊销；设备上限 3 台（可配置），满员需手动腾位；「停止远程」一键断开一切。
- **边界**：无密码输入框（无爆破面）；配对与吊销全程留痕可审计。

### P7 · 固定地址与通道（一期实现范围定死）

- **需求**：有公网域名的用户获得永不变化的接入地址；没有公网的用户开箱即用。
- **一期实现范围**：
  - ✅ **直连域名**（内置）：绑端口供用户反向代理，地址固定——本项目用户的主路径；
  - ✅ **临时隧道**（内置）：自动拉起 Cloudflare 免费隧道，零配置扫码即用；地址随 MAM 重启变化，界面明示当前地址并在变化时桌面通知；隧道程序首次启用时自动下载，下载失败可手动放置（固定旁路机制）；
  - ✅ **命名隧道**（内置）：填入用户自有 Cloudflare Tunnel Token 即得固定地址；
  - ✅ **Tailscale**（仅文档指引，不写代码）：接入指引写入设置页帮助。
- **边界**：四模式共用同一套功能与配对安全语义；直接对外绑端口前，界面强制确认已具备加密前置，否则仅允许本机模式。

### P8 · 移动看板

- **需求**：手机看到与桌面同源同义的八工具会话看板。
- **输入**：已配对设备的访问。
- **输出与效果**：移动版会话卡片（三色/项目/预览/时长），按"等待中→运行中→空闲"排序，可按工具过滤；数据与桌面同源同份；页面可加到手机主屏幕全屏使用。

### P9 · 会话内容查看

- **需求**：点开任意会话卡片可阅读完整对话记录。
- **输入**：卡片点击。
- **输出与效果**：只读对话视图（用户/助手消息流、工具调用摘要折叠），覆盖全部八工具（含 dsh 与无头会话）；不可编辑、不可发送。

### P10 · 实时提醒

- **需求**：状态变化 2 秒级到达手机；页面开与关都有通知路径。
- **输入**：状态跃迁事件（与桌面通知同源同去重）。
- **输出与效果（一期交付双出口，均默认关闭、按需开启）**：
  - 页面开：实时横幅 + 提示音 + 振动；
  - 页面关：系统级推送——**ntfy**（Android/华为，默认出口）与 **Bark**（iOS）两个推送出口同期交付，同一事件源；推送服务器地址支持自定义（默认公共 ntfy，隐私提示文案随附设置页）；
  - 提醒内容含工具、项目、变化方向与消息预览；同会话 10 秒内重复变化合并；等待状态不重复轰击。

### P11 · 电源保活

- **需求**：远程开启期间电脑不得因休眠失联。
- **输入**：远程开关状态。
- **输出与效果**：开启远程即阻止系统与磁盘休眠（屏幕可熄灭）；Windows 下代设硬盘休眠设置并在关闭远程时还原原值；设置页明示合盖/电池模式边界；开关默认开启。

### P12 · APK 壳（一期 M5 收尾里程碑）

- **需求**：网页链路验收后，交付安卓/华为原生壳版本（宪法 D15）。
- **输入**：与网页版完全相同的后端服务。
- **输出与效果**：同一移动界面的 APK——**内置长连接推送（定案）**：APK 与 MAM 推送网关保持自有长连接，页面关闭仍收系统通知，通知归属 MAM 自身、点击直达具体会话；不注册任何推送厂商账号，不依赖第三方推送应用；后台存活采用前台服务 + 电池白名单引导页。
- **边界**：纯壳复用，无独立功能；M4 验收前不动工；HMS 原生推送为一期外远期项。

**第二部分验收（= 宪法 §5.b 一期标准）**：外网手机扫码→八工具看板与任意会话内容；杀掉一个会话→手机 2 秒内提醒；未批准的新设备进不来；吊销/停止立即失效；临时隧道断网自愈；保活期间彻夜不失联；APK 在华为机收到 MAM 自有通知并点击直达。

---

## 4. 输入输出总表

| 需求 | 输入 | 输出 / 效果 |
|---|---|---|
| P1 会话发现 | dsh 进程/索引/日志 | 八工具看板新增 dsh 卡片（子任务不出卡） |
| P2 三色状态 | 回合边界/审批/结束原因 | 六种结束与等待形态的定死映射，接入现有通知链 |
| P3 预览 | 会话消息流 | 真人输入与助手回复；注入过滤；工具帧显示「执行 <工具名>」 |
| P4 跳转 | 卡片点击 | 聚焦/打开 dsh 界面，免重登；失败有提示 |
| P5 兼容降级 | 多代际格式 | 存量可见；未知格式降级卡不崩 |
| P6 配对 | 接入请求+桌面批准 | 审批（默认）+直通（开关）双方式；一次配对 180 天；花名册可吊销 |
| P7 通道 | 用户网络条件 | 直连/临时/命名隧道内置，Tailscale 文档指引；功能与通道无关 |
| P8 看板 | 已配对访问 | 移动版八工具看板，桌面同源 |
| P9 内容 | 卡片点击 | 八工具只读对话视图 |
| P10 提醒 | 状态跃迁 | 2 秒级页面提醒 + ntfy/Bark 双出口系统推送（默认关） |
| P11 保活 | 远程开关 | 远程期间不休眠不失联，关闭还原 |
| P12 APK | 同一后端 | 内置长连接推送的原生壳，点击直达会话 |

## 5. 非功能需求

- **安全**：批准制是唯一入网途径；全程加密（通道侧终止）；最小暴露（不依赖任何第三方插件的开放端点）；配对与吊销全程审计。
- **性能**：状态跃迁到手机 ≤2 秒；看板首屏 ≤1 秒；监控扫描不拖慢桌面现有体验（宪法原则 5）。
- **可靠性**：隧道断线自愈；实时流不可用自动降级轮询；MAM 重启后已配对设备免重连。
- **体验**：中文界面与 MAM 一致；移动页面可安装到主屏幕。

## 6. 里程碑（全部属一期范围，顺序固定；状态标记：✅ 完成 · 🔄 进行中 · ⬜ 未开始）

| # | 交付 | 出口标准 |
|---|---|---|
| M0 ✅ | dsh 探测 | 报告+独立评审落盘（判定：可用附修正） |
| M1 🔄 | dsh 监控底座 | §2 验收全过（第 8 工具监测效果验证）——**代码完成待合并**（2026-09-14：R1–R3 评审修复中，随后手动验收） |
| M2 | 接入骨架 + 局域网看板 | 手机同 WiFi 经配对后看到八工具看板 |
| M3 | 实时与内容 | 状态变化 2 秒到手机；可点开任意会话阅读 |
| M4 | 外网全链路 | 宪法一期验收全绿（外网接入+提醒+配对管控+保活） |
| M5 | APK 壳 | 华为机收到 MAM 自有系统通知并点击直达会话（M4 验收后启动） |

## 7. 证据台账（每项需求的实证强度与验证点）

> 等级：**A 已实证**（本机探测/实测/生产代码）· **B 强参照**（完整源码通读+生产使用先例）· **C 参照**（生态同类实现/官方标准机制，本机未跑）· **D 自研**（新设计，无外部参照，验证点已排入里程碑）。

| 需求 | 等级 | 依据 / 缺口与验证点 |
|---|---|---|
| P1 会话发现 | A | M0 探测实测 |
| P2 三色状态 | A- | 5 状态样本+锁语义实测；interrupted 锁交叉判定端到端验证 = M1 验收"强杀进程"场景；aborted/blocked/max-tokens 无样本（保守映射，版本门维护） |
| P3 预览 | A | M0 实测（注入过滤样本在案） |
| P4 跳转 | A- | macOS Chrome/Safari 实测通过；Windows 标签聚焦随 Windows 支持期验证 |
| P5 兼容降级 | A | 三代际实测；未知格式降级为防御设计 |
| P6 直通配对 | B | dsh-remote-web-ui pairing 源码通读 + 本机生产使用 |
| P6 审批配对+4 位码 | D | 自研交互（两端自有代码，无探测不确定性）；M2 交付最简实现即验证 |
| P7 直连域名 | B | 生态通用架构 + 用户环境具备；服务器实现有同构参照 |
| P7 临时隧道 | A- | 隧道管理源码通读 + 本机生产使用 |
| P7 命名隧道 | C | cloudflared 官方机制 + 生态用例；Token 流程 M4 实测 |
| P8 移动看板 | B | 数据源为生产代码；华为"加到桌面"官方支持已查证 |
| P9 内容只读 | A | dsh zstd 解码实测 + 其余七工具现役 parser |
| P10 页面内提醒 | B | SSE+降级源码 + 生产使用 |
| P10 ntfy/Bark 推送 | C | 生态生产实证；华为机到达率 M4 前 5 分钟实测（设置页测试按钮即此用途） |
| P10 APK 长连接推送 | D | 自研（常规技术+国产 ROM 保活难点，无生态先例）；**M5 首任务原型验证** |
| P11 电源保活 | C | OS 标准机制 + ZCode 同类先例 |
| P12 APK 壳 | C- | Capacitor 成熟框架；本项目打包链路 M5 首任务 hello-world 壳验证 |

**形态覆盖**：P1–P11 网页形态全部可达；APK 形态经同一 WebView + 自研长连接可达；iOS 一期不做原生（PWA + Bark 可选）。

**实现细节统一入口**：`research/refs/phase1-接入层/一期实现要点-技术备忘.md`（M0 报告 F1–F16、独立评审、dsh-remote-web-ui 源码摘要、通道选型要点、生态九模式报告的引用关系见 research/README.md 索引）。


---

## 附录 · 远程消息接收功能进度表（随里程碑更新）

> 分层视角：消息源（状态判定）→ 接入层 → 手机触达。状态：✅ 完成/在产 · 🔄 进行中 · ⬜ 未开始。更新规则：每个里程碑验收后由评审侧更新本表。

### A · 消息源层

| # | 功能点 | 里程碑 | 状态 | 备注 |
|---|---|---|---|---|
| A1 | 八工具会话状态判定（红黄绿 + 中断/取消/blocked 六形态） | M1 | 🔄 代码完成待合并 | dsh 为第 8 家；5 个黄金夹具锁定映射 |
| A2 | 桌面通知/提示音/去重（远程提醒与之同源，P10） | 既有 | ✅ 在产 | |
| A3 | SessionWatcher 后端事件桥（快照 diff → 广播） | M3 | ⬜ | 远程接收的唯一事件源；依赖单飞护栏（M1 评审 R3） |

### B · 接入层

| # | 功能点 | 里程碑 | 状态 | 备注 |
|---|---|---|---|---|
| B1 | axum 服务器 + 移动页骨架 + 设备门禁 | M2 | ⬜ | 局域网先跑通 |
| B2 | 审批配对 + 直通配对（P6） | M4 | ⬜ | 自研交互（证据 D 级）；M2 交最简版 |
| B3 | 通道：直连域名 | M2 | ⬜ | 用户主路径，地址固定 |
| B4 | 通道：临时/命名隧道 + Tailscale 指引（P7） | M4 | ⬜ | quick tunnel 有生产在用参照 |
| B5 | 电源保活（P11） | M4 | ⬜ | |

### C · 手机触达层

| # | 功能点 | 里程碑 | 状态 | 备注 |
|---|---|---|---|---|
| C1 | 移动看板 + 页面开时实时横幅/声音/振动（SSE + 轮询降级，P10a） | M2/M3 | ⬜ | 强参照（dsh-remote-web-ui 源码 + 生产使用） |
| C2 | 会话内容只读（P9） | M3 | ⬜ | 读通道八工具已就绪（A1 即地基） |
| C3 | 页面关时系统推送（ntfy/Bark 双出口，P10b） | M4 | ⬜ | 华为到达率 M4 前实测（证据 C 级） |
| C4 | APK 壳 + 内置长连接推送 + 点击直达（P12） | M5 | ⬜ | 唯一无参照自研项（证据 D 级），M5 首任务原型 |

**进度基线（2026-09-14）**：12 个功能点 = 1 在产 + 1 代码完成待合并 + 10 未开始；M0 ✅ / M1 🔄 / M2–M5 ⬜。

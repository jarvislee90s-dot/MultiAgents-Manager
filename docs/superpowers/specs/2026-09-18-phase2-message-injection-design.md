# 二期设计说明书 · 消息注入与移动操控（可写）v1

> 上位文档：`docs/MASTER-PLAN.md`（项目宪法，D1–D18）。冲突时以宪法为准。
> 版本：2026-09-18 v1。本期裁决（用户三轮对齐，2026-09-18）：全量一册 / 终端注入先行 / Windows 同期纳入（宪法 D18）/ 斜杠命令原样放行 / PIN 即信任 / 多行 `\n` 归一 / 无头五家一次做齐 / 审批应答前置。
> **2026-09-18 v1.1**（用户审阅反馈修订）：①无头通道必须承接审批应答且永不静默卡死（裁决 9）；②W2 flush 口径更名「可输入态」并大白话化；③W9 增加交接模板体系（工作类型 × 工作进度 组合预设 + 默认兜底，裁决 10）；④M6–M10 确定为一个批次一体设计。
> **2026-09-18 v1.2**（用户二审裁决）：①无头通道后移至 M12（仅先于 APK 套壳），首批 = M6–M9（裁决 11）；②队列支持「立即发送」插队（裁决 12）；③审批边界定档——会话内应答（含计划模式批准）属二期、权限模式主动切换属三期、APP 形态应答登记延后（裁决 13）；④新增附录 C 延后项登记表。
> **2026-09-18 v1.3**（用户三审裁决）：①W9 分类矩阵定案——封闭枚举 + 组件拼装 + 三级选择机制（显式选择→自动预判→公共兜底，裁决 15）；②W9.1 由「建议参考」改为**做/不做决策表**；③扫描全篇悬而未决处并定死：LLM 执行器=无头 M12（注入自总结降为延后备选）、JSON 附本默认生成可关、无头排队=端点层会话串行+排队回执、路由规则按宿主形态（工具无关）。
> **2026-09-18 v1.4**（用户四审裁决）：①**推送网关（W11）与 APK 壳/manualChunks（W12）移至三期收尾**（宪法 D15/D17 再修订；网页版体验已足够好，不紧急）——二期终点收敛为 M11 无头（裁决 16）；②**交接执行器定案 = 注入自总结**（会话本体 Agent 执行，零额外 Agent/LLM 配置；独立 LLM 执行器不做——全量 session 首次输入开销；裁决 17）——交接导出 M10 双档完整交付，不依赖无头；③交接面板交互定死（两级单选联动）；④收割流程定死（项目根落盘→MAM 自动转移入库）。
> **2026-09-18 v1.5**（M6R 复核裁决；批次设计 `2026-09-18-phase2-m6r-m9r-injection-hardening-design.md`）：①**M6「消费墙」结论经修正版复核推翻**（官方文档 + conhost/ReactOS 源码三重实证：输入缓冲无界、错误 122 非 OS 层语义；调研沉淀 `research/refs/phase2-消息注入/`）——Windows 通道按同场景先例 cc-discord-remote 实证配方重写，**写入确认为必要件**；②**裁决 18**：A 主通道=终端注入重写后为主力（W3 原样成立）/ B 兜底=无头（M11 排期不动）+ 新增 W13 一键 resume 窗口 / 文件指针长消息方案延后（附录 C #14）/ 未取证映射严格档不出键；③**裁决 19**：远程关闭=冻结队列、重开续跑（W2 注记）；④W9 追加 MAM 落盘文件族保留期管理（M10 落地）。
> 鉴权基座：M5 访问密码制（4 位 PIN + 设备指纹 + per-IP 限速，2026-09-17 用户裁决，M4 spec 追记）——**一切写端点位于已配对设备门禁之后**，无独立写授权层。
> 实现细节（模块/函数/接口/算法）一律不在本文档，统一入口：`research/refs/phase2-消息注入/`。
> 状态：待用户最终确认后进入实施计划。

---

## 1. 目标与范围

二期交付一条主线：**手机给特定会话发消息，agent 真实收到并处理，桌面自然可见，不打断运行中的会话**。按交付顺序展开为四块：

1. **已开始的对话框（终端里的 TUI）**——终端注入引擎直达：macOS 三通道（tmux/iTerm2/Terminal.app）+ Windows 终端通道（D18），打字+回车语义，实时可见；
2. **等待批准的红卡**——手机一键应答（注入对应按键，D12 第二层）；
3. **任务交接**——会话摘要导出 HANDOFF.md（F2.7，双档：注入自总结 + 规则摘要兜底）；
4. **未开窗的会话**——无头通道五家一次做齐（zcode/claude/codex/kimi/opencode，M11 二期收尾后移）。

**已移出二期**：推送网关（Bark/ntfy）与 APK 壳/manualChunks——三期收尾（裁决 16，宪法 D15/D17 再修订）。

**二期不做（边界，非选项）**：切模型/切模式（F3.1 结构化选择器 UI 属三期；斜杠命令**文本**原样放行，见 W4）；会话管理操作；自动流转导入侧（ImportService 属三期 F3.7）；绕过各 harness 自身权限体系；keystroke/accessibility 正式功能（仅 W10 文档级应急预案，D7）。

**分期递进对位**：一期只读（已收官）→ 二期白名单消息（本册）→ 三期受控操控。每次里程碑扩权独立评审（宪法横切原则 1）。

### 1.1 本期裁决记录（2026-09-18 对齐，实施不得重议）

| # | 裁决 | 落档 |
|---|---|---|
| 1 | spec 全量一册（F2.1–F2.10 + D15/D17 移入项），里程碑切分 | 本册结构 |
| 2 | 终端注入先行（M7），无头排后（M9） | §7 里程碑 |
| 3 | Windows 终端注入同期入二期（M6 探测 + M10 实现） | 宪法 D18 |
| 4 | 斜杠命令文本原样放行，写审计留痕（三期边界=结构化 UI，非文本拦截） | W4 |
| 5 | PIN 即信任：已配对设备全量可发，无额外全局/会话级开关 | W4 |
| 6 | 多行语义：手机多行输入 → 传输层字面 `\n` 拼接 → 单行「打字+回车」注入；桌面不换行展示=已知限制（后续可升级多行展示） | W4 |
| 7 | 无头五家一次做齐：zcode/claude/codex/kimi + opencode（附 A 矩阵 ✅ 并入）；OpenClaw 走后续里程碑（gateway 模式另评） | W7 |
| 8 | 审批应答前置（M8，复用注入引擎、增量小、高频刚需），先于无头 | §7 里程碑 |
| 9 | **无头通道必须承接审批应答**（否则 turn 卡死）：claude 经 `--permission-prompt-tool stdio` 双向控制消息应答（AionCore 实证）；无交互审批面的家（codex exec/kimi/opencode）按审批策略驱动、turn 不阻塞 + watchdog 超时兜底——三机制必居其一，无头永不静默卡死 | W6/W7 |
| 10 | **交接模板体系**：LLM 摘要提示词按「工作类型 × 工作进度」组合预设（编码/设计/文档 × 所处环节），未选择时用默认兜底模板；近期只做预设，用户自定义（设置面板配置模板）为远期规划不入本期 | W9 |
| 11 | **无头通道后移至二期末段**（原定 M12 仅先于 APK 套壳——近期需求不强；**v1.4 修订：推送/APK 移三期后，无头 = M11 二期收尾**）；**首批 = M6–M9**（Windows 探测 / macOS 终端注入全链 / 审批应答 / Windows 注入实现） | §8 里程碑 |
| 12 | **队列「立即发送」插队**：任一排队项可手动越过状态门即刻注入（运行中注入进入 agent 自身输入缓冲）；「按最新内容发」= 撤回旧项 + 立即发送新项；各家运行中注入行为 M7 实测 | W2 |
| 13 | **审批边界三档**：①会话内审批应答（Yes/No/选项/「不要再问」/计划模式批准执行/发消息改计划）属二期本期；②权限模式主动切换（计划模式↔完全访问↔需人批）属三期 F3.1；③APP 形态应答仅 computer-use/keystroke 可达（D7 应急级）→ 本期不做登记延后 | W6 |
| 14 | **由简到繁排序**：通道（tmux→iTerm2→Terminal）与工具（Claude/Codex 先行）均先做简单的，复杂的跑通前面再看 | M7/M8 |
| 15 | **W9 分类矩阵定案**：工作类型（编码/设计/文档，封闭枚举）× 各类型专属进度环节轴（共 10 格）**提前预备**；提示词=公共骨架+类型模块+进度模块**组件拼装**；选择机制三级——用户显式选择 → 未选择自动预判（启发式）→ 不满足分类走**公共默认模板**；参考功能按 W9.1 决策表逐项做/不做定档 | W9 |
| 16 | **推送网关（W11）与 APK 壳/manualChunks（W12）移至三期收尾**：网页版体验已足够好，系统级推送与套壳不紧急；宪法 D15/D17 再修订同步落档；二期终点 = M11 无头通道 | W11/W12 |
| 17 | **交接执行器 = 注入自总结**：由该会话正在对话的 Agent 本体执行（已持有全部上下文）——零额外 Agent/LLM 配置；**独立 LLM 摘要执行器不做**（需把全量 session 喂新模型，首次输入开销与配置成本不可取）；Agent 在项目根写 HANDOFF（多数 Agent 仅有项目目录写权限）→ MAM 自动检测并转移入 `~/.mam/handoffs/`；双档 = 注入自总结（默认）+ 规则摘要（兜底）；交接面板两级单选联动（类型→主环节，不支持多选） | W9 |
| 18 | **注入通道架构定案（M6R 复核后，2026-09-18）**：A 主通道=终端注入（Windows 按 cc-discord-remote 实证配方重写：分片节流、注入层不截断、写入确认双层、事件构造实证规格、进程级串行+附加自动复位）——W3 原样成立；B 兜底=无头通道（M11 排期不动）+ W13 一键 resume 窗口（M6R–M9R 批次交付）；文件指针长消息方案延后（附录 C #14）；未取证审批映射**严格档**不出键（对齐 W6「映射缺失不发错误键位」） | W8/W13/附录 C |
| 19 | **远程关闭=冻结队列（2026-09-18）**：「停止远程」语义=切断一切远程操控——投递循环随服务同停，pending 冻结、重开续跑；停服瞬间「已注入未落账」窗口由启动对账补投兜底；启动对账 + 周期兜底扫描（纯 SQL 计数门控，守宪法扫描预算契约） | W2 |

### 1.2 与既有底座的关系（零新地基）

写通道全部复用一期在产底座：`/m/api` 门禁框架（新端点即插）、PIN 设备会话与设备指纹（M5）、SessionWatcher 状态跃迁（队列触发源）、SSE 事件流（回执与队列状态刷新）、`window/` 终端定位链路（tty/pane，跳转功能在用）、各 adapter 会话宿主形态判定（路由表输入）。

## 2. 写通道总体架构

```
手机输入框（会话详情页，多行）
   │ POST /m/api/v1/session-send（PIN 配对设备 cookie 过闸，沿 v1 前缀惯例）
   ▼
注入路由表（会话宿主形态 → 通道决策 + 有头可见性预期）
   ├─ 终端注入引擎（tmux / iTerm2 / Terminal.app / Windows）→ 打字+回车，实时可见
   ├─ 无头通道（zcode / claude / codex / kimi / opencode）→ 子进程一次 turn，回执返回
   └─ 不可注入（WorkBuddy 黑盒 / dsh 待评 / APP 形态）→ 输入框禁用 + 原因说明
   ▼
状态门控队列（黄排队 / 可输入态逐条 flush / 单会话串行 / 可查可撤回）
   ▼
写审计表（SQLite：设备 / 时间 / 会话 / 通道 / 动作 / 内容摘要）→ 发送回执
```

---

## 3. 第一部分 · 发消息主线（W1–W5，对应宪法 F2.1/F2.2/F2.5/F2.6/F2.10）

### W1 · 终端注入引擎（macOS 三通道）

- **需求**：终端里已开的 TUI 会话收到手机消息——打字+回车语义，桌面终端实时可见。
- **输入**：注入请求（session_id、归一后文本、设备花名）；路由表解析出的宿主终端与定位键（tmux pane / tty）。
- **输出与效果（二期行为定死）**：
  - 三通道：tmux `send-keys -t <pane> -l --` + Enter（`-l` 字面量必需，杜绝 `:` 开头被解析为键名）；iTerm2 `write text`（先写文本再补回车，分两步可校验）；Terminal.app `do script ... in window`（转义反斜杠/引号）；
  - 消息标记：`[mobile <设备花名>] ` 前缀，桌面一眼可辨来源；
  - 纯文本语义：send-keys -l 与 write text 均按字面传递，不拼接 shell 元字符转义逻辑；不用 paste-buffer（TUI 粘贴确认行为语义随版本变）；
  - 定位复用 `window/` 既有链路（跳转功能同源），无新定位逻辑。
- **边界**：keystroke/accessibility 仅应急预案（W10/D7）；定位失败（终端已关闭/会话已死）返回明确失败回执，不入队重试。

### W2 · 状态门控队列

- **需求**：不打断正在运行的会话——黄状态消息排队，agent 空下来能收输入时自动放出。
- **输入**：注入请求 + 会话当前状态（SessionWatcher 提供）。
- **输出与效果**：
  - 黄（运行中）= 入队（SQLite 队列表，含设备花名/时间戳/内容）；
  - **会话回到「可输入态」即逐条 flush**。可输入态 = 红·等待（等批准/等输入）**或** 绿·完成/空闲——通俗说：agent 停下来把光标交回输入框的任何时刻（无论它是在等你批准，还是任务做完了）。宪法原文只写了「转红 flush」，但任务完成（转绿）时 agent 同样停在输入框前等下一条指令，若只在转红时放行，完成场景的排队消息会一直压着不发，所以要扩成两种状态都放（宪法口径的完整化，非冲突）；
  - 一次只放一条，发出后重新等待下一个可输入态 = 单会话串行（天然限速）；
  - 红·中断（写入者已死，终端里没人接消息）不 flush：队列挂起，移动端明示「会话已中断，消息保留」；
  - 移动端可查队列（排队中条目+序）可撤回未发；**「立即发送」插队（裁决 12）**：任一排队项可手动越过状态门即刻注入——运行中的 TUI 会把消息放进它自身的输入缓冲（Claude Code 官方支持运行中排队输入），各家运行中注入行为 = M7 实测确认项；「按最新内容发」= 撤回旧项 + 立即发送新项；队列持久化，MAM 重启不丢。
- **远程关闭=冻结队列（裁决 19，v1.5）**：「停止远程」时投递循环随服务同停，pending 冻结、重开续跑；停服瞬间「已注入未落账」窗口由启动对账补投兜底（M6R–M9R 批次 R3）。
- **边界**：队列仅作用于终端注入通道（无头通道自身按 turn 串行，不入队）；flush 失败（终端消失）逐条降级为失败回执。

### W3 · 注入路由表

- **需求**：每个会话该走哪条通道，手机上一目了然。
- **输入**：会话的宿主形态（MAM 已区分 CLI/APP/终端类型与进程信息）。
- **输出与效果**：通道决策（tmux/iTerm2/Terminal/Windows/ZCode 无头/通用无头/不可注入）+ **有头可见性预期**（实时 / 刷新后 / 不可见）返回移动端展示；不可注入的会话（WorkBuddy 全系、dsh 待评、APP 形态）输入框禁用并说明原因。**路由规则按宿主形态判定，工具无关（v1.3 定死）**：凡宿主形态为终端（tmux/iTerm2/Terminal/Windows 终端）的会话一律优先终端注入——无头 resume 对已开 TUI 会话会分叉出后台续写，开着的界面不实时可见。
- **边界**：路由表是纯决策件（会话属性 → 通道），不带副作用；ZCode 桌面形态固定路由无头（在册限定，D6）。

### W4 · 移动端发消息（会话详情页）

- **需求**：手机上有自然的多行输入体验，发送后知道「到了哪一步」。
- **输入**：会话详情页输入框（多行，键盘回车=换行，**发送=按钮**）。
- **输出与效果**：
  - 多行归一：发送时换行符替换为字面 `\n`（两个字符）拼接为单行 → 走 W1 单行注入；输入端看起来多行，agent 与大模型按 `\n` 理解换行；**已知限制**：桌面终端与 MAM 详情页不换行展示（字面 `\n` 长串），后续里程碑可升级多行展示；
  - 队列状态指示：排队中（第 N 条）/ 已送达终端 / 失败可重试；
  - 回执语义（定死）：「已送达」= 注入动作成功（终端收到输入）；「agent 已处理」不由回执表达，由状态跃迁/内容流自然体现；
  - 斜杠命令文本原样放行（`/model` 等不拦截不审查），写审计留痕——三期 F3.1 的边界是结构化选择器 UI，不是文本拦截；
  - 授权面 = PIN 配对设备即信任，无额外开关。
- **边界**：不做出站内容过滤（用户对自己设备负责）；单会话发送受 W2 串行约束。

### W5 · 写操作审计

- **需求**：每一次写动作可追溯。
- **输入**：注入 / 队列 / flush / 撤回 / 审批应答 / 无头驱动，逐条。
- **输出与效果**：SQLite 审计表只增不改——设备（id+花名）、时间、目标会话（工具+session_id+项目）、通道、动作、内容摘要（首 N 字符）、结果；桌面设置页提供查看入口。
- **边界**：审计不含全文（摘要即可定位，避免审计库膨胀）；移动端不暴露审计查询（桌面侧能力）。

---

## 4. 第二部分 · 通道扩展（W6–W8，对应宪法 F2.9/F2.3/F2.4/F2.1 扩展）

### W6 · 审批应答（通道统一，D12 第二层）

- **需求**：会话红·等待批准时，手机上直接批准/拒绝，不用回电脑——**终端会话与无头会话都要能应答**（无头不应答 turn 就卡死）。
- **输入**：审批请求的识别信号——终端会话 = 红卡状态 + 解析器识别审批提示（各家提示形态探测入库）；无头会话 = headless 进程的审批事件（claude `control_request{can_use_tool}`）。
- **输出与效果**：
  - **移动端结构化选项卡是统一 UI**（批准 / 拒绝 / 选项 N / 「不要再问」），机制按通道分派：
    - **终端注入通道（TUI）**：选择后注入对应按键（复用注入引擎，键位而非文本）；**按键映射表：每 CLI 一次探测入库 + 版本探针**——CLI 版本变化提示复核，映射缺失时降级为普通发消息（不发错误键位）；首批 Claude/Codex 只做批准/拒绝，「不要再问」等扩展键位随映射表由简到繁跟进；
    - **无头通道（claude）**：经 `--permission-prompt-tool stdio` 双向控制消息应答（AionCore 实证：claude 发 `control_request` 后阻塞，宿主回 `control_response` allow/deny）——选项卡选择 → 控制消息；
    - **无头通道（codex exec / kimi / opencode）**：无交互审批面 = **审批策略驱动**（spawn 时给定 approval policy，turn 不因审批而阻塞；拒绝/需批准事件回流到回执，用户可调策略后重试）；
  - **watchdog 超时兜底**：无头 turn 超时（默认可配置）自动回收进程并回报超时回执——设计保证「双向应答 / 策略驱动不阻塞 / watchdog」三机制必居其一，**无头 turn 永不静默卡死**。
- **审批边界三档（裁决 13）**：①会话内审批应答属**二期本期**——含工具权限批准（Yes/No/选项/「不要再问」）与**计划模式**的两类操作（发消息继续改计划 = 普通发消息走 M7 注入；点「执行计划」= 批准键注入走 M8）；②权限/模式**主动切换**（计划模式 ↔ 完全访问 ↔ 需人批的设定）属三期 F3.1 切模式，本期不做；③APP 形态会话（WorkBuddy/Codex APP 等）无外部写 API，唯一通路 computer-use/keystroke（需解锁+抢焦点，宪法 D7 应急预案级）→ **本期不做**，登记延后表（附录 C）。
- **边界**：TUI 按键映射首批 Claude/Codex 探测入库（M8），其余工具按探测结论由简到繁跟进；claude 无头双向通道随 M11 无头通道落地（首批预留选项卡→控制消息分派接口）；终端注入为应用级脚本（tmux/AppleScript），**不需解锁也不抢焦点**——锁屏可用性 = M7 实测确认项；协议原生审批通道（app-server/ACP request_permission）属三期 D12 第一层，本期不碰。

### W7 · 无头通道（五家一次做齐 · 里程碑 M11 二期收尾后移，裁决 11）

- **需求**：未开窗的会话也能被驱动（R2.4）；一次 turn 的结果有回执。**近期需求不强，后移至二期收尾（M11，现为期末里程碑）**。
- **输入**：session_id + 文本（多行同样 `\n` 归一，与 W4 统一语义）。
- **输出与效果**：
  - 五家通道：`zcode --resume <id> --prompt <text> --cwd <项目> --json`（**限定在册工作区**，D6 判定 F——桌面刷新后可见）/ `claude -p --resume <session> --output-format stream-json`（配 `--permission-prompt-tool stdio` 承接审批应答，见 W6）/ `codex exec resume <id>`（审批策略驱动，turn 不阻塞）/ `kimi -p -S <id>`（恢复含运行时状态）/ `opencode run`（附 A 矩阵 ✅ 并入）；
  - 进程管理按 **AionCore 骨架**：Spawner trait、进程注册表、空闲挂起、崩溃重生、版本门控；
  - 回执：turn 完成返回末条 assistant 消息 + token 用量；无头产生的新回合经读链路在移动端与桌面 MAM 自然可见；
  - 单会话同时至多一个无头 turn：**端点层按会话串行（v1.3 定死）**——新请求在已有 turn 进行中时返回「排队回执」（含队列位置），turn 结束自动依序执行；审批事件按 W6 分派（claude 双向应答 / 其余策略驱动）+ watchdog 超时兜底。
- **边界**：无头对已开 TUI 的会话默认不路由（W3）；Codex 无头追加后桌面索引不保证刷新（官方 #28259，已知限制明示）；zcode / kimi / opencode 的无头审批与回执行为 = M11 首任务逐家探测确认；OpenClaw 无头（sessions CLI）走后续里程碑，gateway 模式另评。

### W8 · Windows 终端注入（D18）

- **需求**：Windows 实机（mam-win 场景）上的终端会话同样可发消息。
- **输入**：M6 探测里程碑的结论（写入路径定案）。
- **输出与效果**：Windows 终端写入通道（候选：WriteConsoleInput / ConPTY 附加写入 / Windows Terminal 通道，以探测报告为准）；与 macOS 通道同口径的行为（`[mobile]` 标记、`\n` 归一、队列、审计）与同口径验收。
- **边界**：M6 探测报告 + 独立评审先行（类似一期 M0 模式），探测不通过则该里程碑降级登记为已知限制并回报用户裁决。
- **v1.5 注记（M6R 复核）**：M6「消费墙」机制学结论被推翻（输入缓冲无界 / 错误 122 非 OS 层——官方文档与 conhost/ReactOS 三重源码实证；方法论硬伤清单与补测实验见 `research/refs/phase2-消息注入/`《Windows控制台写入语义与消费墙复核》）。Windows 通道按同场景先例 cc-discord-remote 实证配方重写：分片节流（注入层不截断长度）、**写入确认双层**（回车补按 + 会话文件命中）为必要件、事件构造实证规格、进程级串行 + 附加自动复位守卫、未取证审批映射严格档。重写需求与验收见批次设计 M6R–M9R（R2）。

---

## 5. 第三部分 · 交接与触达（W9–W12，对应 F2.7/F2.8 + D15/D17 移入项）

### W9 · 会话交接导出（F2.7）

- **需求**：任务可以交接出去——摘要导出人可读、agent 可续；**下一个 Agent 拿到足够信息无缝衔接是第一目标**。
- **输入**：会话卡「交接」按钮；交接面板（**两级单选联动，裁决 17 定死**）——第一级「工作类型」按钮组（编码 / 设计 / 文档 / 不选），点击类型后第二级**自动展开**该类型对应的环节按钮（如编码 → 需求与方案设计/实现中/待验证/待收尾）；两级各选**一项**（环节为任务**主环节单值**，不支持多选——模板侧重是「加权」不是「裁剪」，九段式全量信息恒在，跨环节任务选主环节即可，选错格子代价低）；自动预判结果预填两项、用户可改、清空任一级 = 不满足分类走公共兜底；确认后点「导出」执行。摘要档位自动：会话可注入 → 注入自总结；不可注入 → 仅规则摘要。
- **输出与效果**：
  - `~/.mam/handoffs/<时间戳>-<来源>-<目标>-<项目>.md`（HANDOFF v1 九段式：Objective / Work Completed / Current State / Decisions & Rationale / Next Steps / Environment & File Context / Session Log / Smart Defaults / Verification Steps + 原始会话指引）+ SQLite 索引表登记；九段式骨架对所有模板恒定（保持 agent 无关的可续格式），**模板作用于 LLM 摘要提示词与各段内容的侧重**，不增删段落；**同名 `.json` 无损附本默认随文件落盘**（设置页可关）；
  - **分类矩阵（提前预备，封闭枚举，裁决 15）**——情形分类必须先于使用建成，共 10 格：

    | 类型 ＼ 进度环节 | ① | ② | ③ | ④ |
    |---|---|---|---|---|
    | **编码** | 需求与方案设计 | 实现中 | 待验证 | 待收尾 |
    | **设计** | 探索与调研 | 方案迭代中 | 已定待评审/制作 | — |
    | **文档** | 大纲搭建 | 内容撰写中 | 待审校 | — |

  - **提示词 = 组件拼装，不写格格全文**：每格提示词 = **公共骨架（1 份）+ 类型模块（3 份）+ 进度模块（10 份）** 组合生成，共 14 个组件覆盖 10 格（新增类型/环节只加组件，不改矩阵机制）；
    - **公共骨架**（所有格子共用）：按九段式产出 HANDOFF.md；强制保留项（见下）；事实源真实引用（文件路径/git 提交不得编造）；面向「下一个执行者无缝接续」行文；
    - **类型模块**（留存偏向）：**编码** = 改动清单化（已完成/进行中/未开始）+ 关键代码**实际签名**（不转述）+ 文件与改动范围 + 测试与构建状态 + 报错原文；**设计** = 目标与约束 + 方案演进史（考虑过什么、为何淘汰）+ 最终方案结构与理由 + 未决权衡；**文档** = 读者与口径 + 大纲结构现状 + 已写/未写章节 + 事实与引用核对状态 + 术语表；
    - **进度模块**（接续点侧重，交接价值 = 这一环节没做完、下一个人能否接上）：编码①=需求理解、候选方案与选型理由、待确认决策点；编码②=进行中模块的接续点（下一动作从哪个文件/函数/分支继续）+ 依赖与阻塞；编码③=验证步骤 + 每步预期结果 + 已知风险与失败路径；编码④=重构点 + 文档/发布清单 + 遗留 TODO；设计①=已调研事实、信息缺口、下一步调研计划；设计②=当前迭代版本、上一轮反馈与差异、待改点；设计③=方案定稿要点、评审关注点、交付物制作进度；文档①=大纲结构、章节目标、素材清单；文档②=当前章节、素材缺口、口径待定项；文档③=审校清单（事实核查/口径统一/术语一致）；
  - **模板选择机制（三级，定死）**：① 用户显式选择（类型 3 选 1 或「不选」，进度轴随类型联动）；② 未选择 → **自动预判**（M10 交付的启发式规则，非 LLM：仓内文件构成〔源码为主→编码倾向 / 设计稿与图床→设计倾向 / md 为主→文档倾向〕× 会话关键词〔构建/测试/编译→编码；方案/评审/草图→设计；章节/审校→文档〕，证据冲突或不足 = 不满足分类；预判结果在面板预填、用户可改）；③ **落格 → 该格组合提示词；不满足分类 → 公共默认模板**；
  - **公共默认模板（兜底正文，定死）**：当前进展与关键决策 / 重要约束与用户偏好 / 剩余工作与清晰下一步 / 关键数据与引用（Codex checkpoint 口径，即四家 compact 共同五要素基线）；
  - **强制保留项（无论哪格）**：失败路径清单（「不要重走」）+ 带预期结果的验证步骤；
  - **双档执行（裁决 17，v1.4 定死）**：
    - **注入自总结（默认档）**：执行者 = **该会话正在对话的 Agent 本体**（已持有全部上下文，零额外配置）。流程五步：① 组装模板提示词（公共骨架 + 类型模块 + 进度模块 + 输出约定）；② 经 **M7 注入通道**发给会话（带 `[mam] ` 前缀与来源标记，受状态门控：黄排队 / 可输入态 flush / 可插队）；③ Agent 在**项目根目录**写 `MAM-HANDOFF-<会话短id>-<时间戳>.md`（多数 Agent 仅有项目目录写权限——这正是落盘位置选项目根的原因）；④ MAM 检测写入完成（回合结束 + 文件出现且尺寸稳定）；⑤ **自动收割转移**至 `~/.mam/handoffs/<规范名>.md` + SQLite 索引登记 + `.json` 附本——无需用户手动命令。超时（默认 10 分钟，可配置）未产出 → 交接失败回执并建议规则摘要兜底；
    - **规则摘要（兜底档）**：零依赖离线（= 极简交接档定位）；会话不可注入（已死 / APP 形态 / 黑盒工具）或自总结超时失败时使用；
    - **独立 LLM 摘要执行器不做**（v1.4 定死）：需把全量 session 喂给另行配置的模型——首次输入 token 开销巨大且引入配置负担，全面劣于让已持有上下文的会话本体直接总结。
- **边界**：导出侧 only——ImportService（投递/确认回执）属三期 F3.7；LLM 摘要在无头通道不可用的工具上自动降级规则摘要；**近期只做预设矩阵，用户自定义模板（设置面板配置侧重点/自建组件）为远期规划不入本期**（裁决 10）；类型与进度枚举**封闭**（不做自由文本自定义），扩类 = 加组件 + 修订本 spec。
- **MAM 落盘文件族保留期管理（v1.5 追加，2026-09-18 用户裁决）**：MAM 自建落盘文件（handoffs 交接件、事件文件、未来可能新增的提示词/附件文件等）统一设计保留期与清理策略（防长期膨胀；现状仅事件文件有 30s TTL），M10 设计时随交接目录一并落地。

#### W9.1 · 参考功能采纳决策表（做/不做，定档）

**调研事实**（2026-09-18，Claude Code / Codex / OpenCode / Amp 一手提示词与实现核对）：四家 compact 全部是单一通用模板，**无一家按工作类型分类**——分类矩阵是 MAM 自研增量；共同保留五要素（已完成/进行中/文件/下一步/约束决策）即公共默认模板基线。社区 handoff 已成一等模式（willseltzer/claude-handoff、AI Hero /handoff、beads 集成等）。

| # | 参考物 | 来源 | 决策 | 落点 |
|---|---|---|---|---|
| 1 | HANDOFF 九段式骨架 | 宪法 §4.(b) / qdhenry 模板 | ✅ **做** | 所有格子共用，恒定不增删段 |
| 2 | 失败路径清单（不要重走） | willseltzer/claude-handoff | ✅ **做** | 强制保留项（全格子） |
| 3 | 带预期结果的验证步骤 | willseltzer + 宪法 Verification Steps 段 | ✅ **做** | 强制保留项（全格子） |
| 4 | 代码实际签名展示（不转述） | willseltzer | ✅ **做** | 编码类型模块（Environment & File Context 段） |
| 5 | 自定义指令参数化机制 | Claude Code `/compact <指令>` | ✅ **做** | 模板即「预置的自定义指令」，机制同构 |
| 6 | 五要素通用压缩口径 | 四家 compact 共同基线 | ✅ **做** | 公共默认模板正文（兜底） |
| 7 | 极简交接档（quick handoff） | willseltzer /quick | ✅ **做** | 规则摘要即此定位（M10 交付档位） |
| 8 | 压缩后保留最近消息原文（~20k tokens） | Codex | ❌ **不做** | 属上下文连续性机制，非交接产物；交接场景由 Session Log 段 + 原始会话指引承担 |
| 9 | 工具输出 prune 分级保留 | OpenCode | ❌ **不做** | 属上下文窗口管理，与交接导出无关 |
| 10 | 按「下一任务目标」抽取 | Amp Handoff | ◐ **只吸收思想** | 进度维度的设计来源；不做独立 goal 参数 |
| 11 | resume/continue 接续命令（回执） | willseltzer / beads / superpowers | ❌ **不做** | Import 侧属三期 F3.7 |
| 12 | beads 任务链集成 | 社区 | ❌ **不做** | 远期（三期后评估，入延后表） |

### W10 · keystroke 应急预案（文档级，F2.8/D7）

- **需求**：万能兜底手段记录在案，不承诺、不默认。
- **输出与效果**：AppleScript keystroke / accessibility 作为应急遥控手段写入文档（适用场景、前置条件、风险：抢焦点/前台要求/中文输入法需切 ABC/自动化权限）；不实现、不暴露 UI。
- **边界**：任何通道失效时的最后手段参考，非正式功能。

### W11 · 推送网关（Bark/ntfy · **三期收尾**，裁决 16 / 宪法 D17 再修订）

- **需求**：页面关着的系统级推送——一期 F1.6 按裁决迁入本期。
- **输入**：状态跃迁事件（SessionWatcher 同源同去重，与 SSE/桌面通知同一套边沿事件）；设置页 Bark/ntfy URL 配置。
- **输出与效果**：**推送网关抽象**（同一事件源多出口）：Bark（iOS）/ ntfy（Android；华为无 GMS 机型默认出口）/ APK 出口（W12 内置长连接）预留；标题=工具+项目，正文=跃迁方向+预览；失败静默重试一次；设置页测试按钮（即华为机到达率实测入口）；默认公共 ntfy 的隐私提示文案随附。
- **边界**：默认关闭按需开启；推送网关不承接写操作回执之外的任何新事件源。

### W12 · APK 壳（**三期收尾**，裁决 16 / 宪法 D15 再修订）

- **需求**：网页链路验收后的原生壳交付。
- **输入**：与网页版完全相同的后端服务。
- **输出与效果**：Capacitor 壳 + **内置长连接推送**（APK 与 MAM 推送网关自有长连接，页面关闭仍收系统通知，通知归属 MAM 自身、点击直达具体会话）+ 前台服务与电池白名单引导页；manualChunks 移动端分包同批（D17 附注）。
- **边界**：纯壳复用无独立功能；**2026-09-18 再修订（裁决 16）：移至三期收尾**，原「二期发消息验收后启动」触发条款作废；不注册推送厂商账号。

### W13 · 一键 resume 窗口（v1.5 新增，裁决 18b；F3.4 前置件提前到二期）

- **需求**：手机或桌面点一下，MAM 在本机自动打开终端、进入项目目录、恢复该会话并聚焦——用户零额外操作。服务 B 兜底（注入走不通的会话经无头驱动后的「可见性」半部）与「回电脑继续」场景。
- **输入**：会话卡/详情页「在电脑上打开」（移动端走 PIN 门禁内端点；桌面端 IPC 命令）；会话元数据（项目目录、会话 id、工具类型）。
- **输出与效果**：本机 spawn 终端（Windows 优先 Windows Terminal、未装回退 conhost；macOS 优先 iTerm2、次选 Terminal.app，复用既有脚本基建）→ cd 项目目录 → 执行该工具 resume 命令（各家映射表探测入库：claude 已知，其余查证）→ 聚焦窗口；不可行情形明示（项目目录缺失→按钮禁用+原因）；写审计（动作「打开」）。
- **边界**：只负责「打开与恢复显示」，不承担消息投递（投递仍走 W1/W8 注入与 M11 无头）；M11 落地前注入不可行会话仅显示原因，B 兜底的「发」半部随 M11 补全。

---

## 6. 输入输出总表

| 需求 | 输入 | 输出 / 效果 |
|---|---|---|
| W1 终端注入 | `/m/api/v1/session-send`（session_id、文本、设备花名）；路由表解析宿主 | tmux send-keys / write text / do script / Windows 通道执行；副作用=目标终端一行 `[mobile 设备名]` 输入+回车 |
| W2 队列 | 注入请求 + 会话状态（Watcher） | 黄：入 SQLite 队列；静息态（红·等待/绿·完成）逐条 flush；单会话串行；可查可撤回；重启不丢 |
| W3 路由表 | 会话宿主形态 | 通道决策 + 有头可见性预期（实时/刷新后/不可见）返回移动端；不可注入禁用+说明 |
| W4 移动端发消息 | 详情页多行输入框 + 发送按钮 | 多行 `\n` 归一单行注入；回执（已送达终端/排队中/失败）；斜杠放行 |
| W5 写审计 | 每次写动作 | SQLite 审计行（设备/时间/会话/通道/动作/摘要/结果），设置页可查 |
| W6 审批应答 | 红卡 + 审批提示识别 | 结构化选项卡 → 注入按键；映射探测入库 + 版本探针 |
| W7 无头 | session_id + 文本 | 五家 CLI 一次 turn；回执=末条 assistant + token 用量；AionCore 骨架管理 |
| W8 Windows 注入 | M6 探测结论 | Windows 终端写入通道；与 macOS 同口径行为与验收 |
| W9 交接导出 | 会话卡交接按钮（规则/LLM 双档） | `~/.mam/handoffs/` HANDOFF.md v1 + 索引表；JSON 附本可选 |
| W10 应急预案 | — | 文档级登记（D7），不实现 |
| W11 推送网关 | 状态跃迁事件 + Bark/ntfy URL 设置 | HTTP POST 推送（标题=工具+项目）；失败静默重试一次；测试按钮 |
| W12 APK | 同一后端 | Capacitor 壳 + 内置长连接推送 + 点击直达；manualChunks 同批 |
| W13 一键 resume 窗口 | 会话卡「在电脑上打开」（双端） | 本机 spawn 终端 cd 项目目录 + resume + 聚焦；不可行明示；写审计 |

## 7. 非功能需求

- **安全**：PIN 门禁覆盖一切写端点（宪法横切 1）；写审计全覆盖、只增不改；消息标记可归因到设备；注入纯文本语义（不拼 shell 元字符）；单会话串行天然限速；无头/注入通道版本门控+探针+降级（横切 6）。
- **性能**：点发送 → 目标终端出现文字 ≤3s（公网隧道链路）；队列 flush 在静息跃迁后 1s 内；无头回执随 turn 完成返回。
- **可靠性**：注入失败明确回执+可重试；队列 SQLite 持久化重启不丢；无头进程崩溃重生；一期能力零回归（横切 5：桌面监控/通知/资源管理不受影响）。
- **体验**：中文界面与 MAM 一致；回执清晰区分「已送达终端」与（由状态流体现的）「agent 已处理」；不可注入会话明示原因；可见性预期（实时/刷新后）在发送前告知。

## 8. 里程碑（全部属二期范围，顺序固定；编号续一期；状态：✅ 完成 · 🔄 进行中 · ⬜ 未开始）

| # | 交付 | 出口标准 |
|---|---|---|
| M6 ⬜ | Windows 注入探测 | 报告 + 独立评审落盘（研究库 phase2 目录），写入路径定案 |
| M7 ⬜ | macOS 终端注入全链 | 宪法二期验收前三条（macOS 侧）：真实会话手机发消息→终端出现 `[mobile 设备名]` 输入（tmux/iTerm2/Terminal 三通道各实测，由简到繁）；黄排队可输入态 flush、可查可撤回、「立即发送」插队实测；审计逐条可查；不可注入卡禁用；锁屏注入可用性确认 |
| M8 ⬜ | 审批应答 | Claude/Codex TUI 按键映射探测入库；红卡一键批准/拒绝→终端按键生效；计划模式「批准执行」可用（裁决 13）；映射缺失降级路径可用；版本探针变更提示 |
| M9 ⬜ | Windows 注入实现 | 依 M6 结论实现；Windows 实机全链 + 真机蜂窝复验 |
| M10 ⬜ | 交接导出 | HANDOFF.md 人可读、agent 可续——**双档完整交付**（注入自总结默认 + 规则摘要兜底）、分类矩阵/自动预判/公共兜底、两级单选面板、收割转移入库与索引、`.json` 附本全部落地（依赖 M7 注入通道，**不依赖无头**） |
| M11 ⬜ | 无头通道五家（二期收尾；后移裁决 11） | ZCode 在册无头发消息→桌面刷新后可见（判定 F 复现）；五家回执（末条 assistant + token 用量）；claude 审批双向实机；挂起/重生可用；watchdog 生效 |
| M6R–M9R 🔄 | 终端注入加固与增强（v1.5 立项：M6 复核重探 → M9 引擎重写 + 修复批 + 审批补全 + W13 resume 窗口） | 补测报告+独立评审落盘；三档长度 × 三 CLI × 双宿主全量送达 + 写入确认实测；P1 修复各带回归测试；codex/claude 审批实机；resume 双端实测。批次设计：`2026-09-18-phase2-m6r-m9r-injection-hardening-design.md` |

**移出二期（裁决 16，2026-09-18）**：W11 推送网关与 W12 APK 壳/manualChunks 移至**三期收尾**（宪法 D15/D17 再修订）——网页版体验已足够好，系统级推送与套壳不紧急。

**二期验收（宪法 §5.b + D18）**：手机对 tmux/iTerm2/Terminal 里的会话发消息→终端出现 `[mobile]` 标记输入；黄状态消息排队、转红 flush；对 ZCode 在册会话无头发消息→桌面刷新后可见（判定 F 复现）；导出 HANDOFF.md 人可读、agent 可续；Windows 终端注入同口径实机复验。收尾：APK 在华为机收到 MAM 自有通知并点击直达会话。

**批次编排（裁决 11，v1.4 修订）**：**首批 = M6–M9**（Windows 探测 / macOS 终端注入全链 / 审批应答 / Windows 注入实现）——一份设计文档（`2026-09-18-phase2-m6-m9-injection-design.md`）+ 一个实施计划一体交付；M6 探测**仅 gate M9**（Windows 注入实现），不阻塞 M7/M8——探测不通过时 M9 降级登记为已知限制，批次其余照常交付。后续批次：M10 交接导出（双档，依赖 M7 注入）→ **M11 无头通道（二期收尾）**；推送网关与 APK 移三期收尾（裁决 16）。通道与工具均**由简到繁**（裁决 14）：tmux→iTerm2→Terminal，Claude/Codex 先行。

## 9. 证据台账（每项需求的实证强度与验证点）

> 等级：**A 已实证**（本机探测/实测/生产代码）· **B 强参照**（完整源码通读+生产使用先例）· **C 参照**（生态同类实现/官方标准机制，本机未跑）· **D 自研**（新设计，无外部参照，验证点已排入里程碑）。

| 需求 | 等级 | 依据 / 缺口与验证点 |
|---|---|---|
| W1 macOS 终端注入 | A- | 三通道 API 要点本机验证 + 官方文档（研究库《终端注入通道-参考实现》）；定位链路 `window/` 在产（跳转功能）；「注入+队列+回执」组合端到端 = M7 首任务真实会话实测 |
| W2 队列/状态门控 | A- | 状态跃迁源 SessionWatcher 在产（M3）；队列状态机自研（D 级验证点排 M7） |
| W3 路由表 | A- | 宿主形态判定 MAM 已有（adapter 在产）；路由决策自研，随 M7 验证 |
| W4 移动端输入 | A | 详情页/门禁/SSE 底座全部在产（一期 P9 + M5 PIN）；`\n` 归一纯函数 = M7 单测锁定 |
| W5 写审计 | A | SQLite 层既有（schema/migration/dao 模式成熟） |
| W6 审批应答（TUI 按键） | D | 按键映射自研；每 CLI 探测入库 + 版本探针 = M8 验证（首批 Claude/Codex） |
| W6 审批应答（无头） | A- | claude `--permission-prompt-tool stdio` 双向通道 AionCore 实证（control_request/control_response）；codex exec/kimi/opencode 策略驱动行为 = M11 逐家探测 |
| W7 ZCode 无头 | A | spike 判定 A/F（0.16.5 本机实证：在册工作区桌面刷新可见） |
| W7 Claude 无头 | B | 官方 `-p --resume --output-format stream-json` + 生态生产使用（AionCore 报告） |
| W7 Codex 无头 | B | 官方 #28259：exec resume 可追加桌面会话 rollout（索引不保证刷新，已知限制） |
| W7 Kimi 无头 | C | 官方 `-p -S <id>` 恢复含运行时状态；本机未跑全链 = M11 首任务验证 |
| W7 OpenCode 无头 | C | 官方 `run` CLI；M11 验证 |
| W7 进程骨架 | B | AionCore 报告深读（Spawner/注册表/挂起/版本门控，main@2026-09-09） |
| W8 Windows 注入 | A- | AttachConsole(pid) 附加 claude ConPTY 实证（跳转 marker 三轮验收 #43）；**写入路径**未证 = M6 探测 |
| W9 交接格式与模板 | B | qdhenry handoff 模板（宪法基线）+ 四家 compact 一手提示词核对（Claude/Codex/OpenCode/Amp，见 W9.1）+ willseltzer/claude-handoff 等社区实现段落实践；组合模板（类型×进度）为自研增量 = M11 验证；LLM 摘要依赖 W7 |
| W11 推送网关 | C | Bark/ntfy 生态生产实证；华为机到达率 = 三期收尾实测（设置页测试按钮即此用途） |
| W12 APK 壳 | C- | Capacitor 成熟框架；打包链路三期收尾首任务 hello-world 壳验证（沿一期 P12 口径） |

**形态覆盖**：W1–W9 网页形态全部可达；W12 APK 经同一 WebView + 自研长连接可达。

## 附录 A · 工具 × 通道矩阵（二期注入范围，随实现更新）

| 工具 | 终端注入(macOS) | 终端注入(Win) | 无头 | 审批应答 | 有头可见性 |
|---|---|---|---|---|---|
| Claude Code | ✅ 三终端 | ✅（M10） | ✅ -p --resume | TUI 探测入库（M8）+ 无头双向通道（M9） | 注入实时 / 无头刷新后 |
| Codex CLI | ✅ | ✅（M10） | ✅ exec resume | 探测入库（M8） | 注入实时 / 无头索引滞后（已知限制） |
| OpenCode | ✅ | ✅（M10） | ✅ run（并入，裁决 7） | 探测跟进 | 官方 web 实时 |
| OpenClaw | ◐ gateway 另评 | ◐ | 后续里程碑（sessions CLI） | — | Gateway 单一状态源 |
| Kimi Code | ✅ | ✅（M10） | ✅ -p -S | 探测跟进 | 刷新后 / 注入直达 |
| WorkBuddy | ❌ 黑盒（输入框禁用） | ❌ | ❌ | ❌ | 仅深链跳转 |
| ZCode | —（走无头） | — | ✅ --resume 在册限定（D6） | — | 在册工作区（判定 F） |
| dsh | ◐ 另评（web API/插件生态） | ◐ | ◐ CLI headless 待探 | — | 自带 web UI |

## 附录 B · 功能点进度表（随里程碑更新）

| # | 功能点 | 里程碑 | 状态 |
|---|---|---|---|
| W1 | 终端注入（macOS） | M7 | ⬜ |
| W2 | 状态门控队列（含立即发送插队） | M7 | ⬜ |
| W3 | 注入路由表 | M7 | ⬜ |
| W4 | 移动端发消息 | M7 | ⬜ |
| W5 | 写操作审计 | M7 | ⬜ |
| W6 | 审批应答（TUI 路径） | M8 | ⬜ |
| W6' | 审批应答（无头双向/策略/watchdog） | M11 | ⬜ |
| W7 | 无头通道五家 | M11 | ⬜ |
| W8 | Windows 注入（探测→实现→复核重写） | M6/M9 + M6R–M9R | 🔄 重写中 |
| W9 | 交接导出（双档：注入自总结+规则兜底） | M10 | ⬜ |
| W10 | keystroke 应急预案（文档） | M10 | ⬜ |
| W13 | 一键 resume 窗口（F3.4 前置件） | M6R–M9R 批次 | ⬜ |
| W11 | 推送网关 | 三期收尾（裁决 16） | ⬜ |
| W12 | APK 壳 + manualChunks | 三期收尾（裁决 16） | ⬜ |

**进度基线（2026-09-18 v1.4）**：二期 ⬜ 全量未开始（**M6–M11 共 6 个里程碑**；推送网关与 APK 移三期收尾，裁决 16）；首批 = M6–M9；一期已收官（M0–M4 合并在产，M4/M5 增强在 PR #68 待合并）。**二期工作基线（09-18 定）= 分支 `feat/phase2-injection`**（最新 main〔含 preset-v2 PR #63〕+ PR #68 全量预演合并，六门禁全绿：cargo 738+ / vitest 526）——PR #68 正式合并后自然归一。

**v1.5 追记（2026-09-18）**：首批 M6–M9 已在 `feat/phase2-injection` 实施交付（本地 30 提交待评审；Windows 实机 FFI 地面真值 + claude 审批键实证取得）；评审 + 五路调研推翻 M6 消费墙结论 → **M6R–M9R 加固批次立项**（探测先行，见批次设计文档）。

## 附录 C · 延后项登记表（活账：每次评审/验收将延后项入账，spec 版本行追记）

| # | 延后项 | 原属 | 现落点 | 原因 | 复核时机 |
|---|---|---|---|---|---|
| 1 | 无头通道五家（含 claude 审批双向、策略驱动、watchdog） | 原 M9 | M11（二期收尾） | 裁决 11：近期需求不强 | M11 启动前 |
| 2 | 交接导出独立 LLM 摘要执行器（读全量 session） | v1.1-v1.3 曾定无头执行 | **不做**（裁决 17 终裁） | 首次输入 token 开销巨大 + 需额外配置 Agent/模型；由注入自总结替代（M10 已交付） | — |
| 3 | APP 形态会话的审批应答（WorkBuddy/Codex APP 等） | W6 范围评估 | 未排期 | 唯一通路 computer-use/keystroke（解锁+抢焦点，D7 应急级不入正式功能） | 三期 F3.3 协议矩阵期 |
| 4 | TUI 按键映射全工具覆盖（OpenCode/Kimi 跟进） | M8 | M8 后跟进 | 裁决 14 由简到繁，首批 Claude/Codex | M8 验收后 |
| 5 | 「不要再问」等扩展键位 | M8 选项 | M8 跟进 | 首批先做批准/拒绝 | 按键映射探测时 |
| 6 | 多行展示升级（桌面换行渲染 `\n` 字面量） | W4 已知限制 | 未排期 | 裁决 6 已接受现方案 | 用户提出时 |
| 7 | 用户自定义交接模板（设置面板） | W9 远期 | 三期后 | 裁决 10 近期只做预设 | 三期 |
| 8 | OpenClaw 无头（sessions CLI）与 gateway 注入 | W7 范围评估 | M11 后 | 裁决 7 gateway 另评 | M11 设计时 |
| 9 | dsh 写通道（web API/插件生态） | 附录 A ◐ | 未排期 | 宪法 D14 写通道另评 | 生态变化时 |
| 10 | ZCode 桌面形态发消息/应答（走无头） | W7 关联 | M11 | 无头后移连带 | M11 设计时 |
| 11 | OpenCode prune 式上下文分级保留 / Codex 最近消息原文并存 | W9.1 决策表 #8/#9 | 不做 | 与交接导出无关（上下文窗口管理） | — |
| 12 | beads 任务链集成 / resume 接续命令 | W9.1 决策表 #11/#12 | 三期后 | Import 侧属三期 F3.7 | 三期 F3.7 设计时 |
| 13 | 推送网关（W11）与 APK 壳/manualChunks（W12） | 二期原 M11/M13 | 三期收尾（裁决 16，D15/D17 再修订） | 网页版体验已足够好，系统级推送与套壳不紧急 | 三期收尾启动前 |
| 14 | 文件指针长消息方案（长提示词落 markdown 至 `~/.mam/prompts/` + 注入短指令引用） | M9R 评估 | 未排期（裁决 18c） | 主通道重写后任意长度可直达；该方案桌面终端阅读残缺（仅见路径不可跳转）；无头通道 stdin 本无长度压力 | 注入层出现长度硬限制或超长文档投递需求时 |

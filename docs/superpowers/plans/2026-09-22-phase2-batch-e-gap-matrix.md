# 批次戊前置 · 远程端审批/问答全能力差距矩阵（四终端）

> 2026-09-22 · 依据：用户三轮手工测试（含本轮 19:56-20:02 codex 多题会话）+ 审计表 + rollout/wire/SQLite 对账 + 状态链代码勘察 + 官方文档调研。
> **目标定义（用户裁定）**：远程端能做到桌面端的所有交互——审批（含 N 选项对话框）、问答（单选/多选/多题）、自由作答（Type something/Other/notes）、模式与权限切换（含回显当前档）、计划展示。**用户在任何卡片状态下都不能被卡死**。
> 本文档 = 差距矩阵 + 新 bug 根因 + 批次戊任务输入。R2（composer 转向 claude / 多选形态裁决 / D20 稳定闸 / 文档项）按用户实测裁决收敛后另行收口。
> **v2（2026-09-22 批次戊 Stage 1 探测后）**：新增 §6 探测定案回填——N1/N4/N5/N6/N7 键序面全部定案，N2 定案且守卫前置升级为硬需求，N8 素材扩充，插队三工具语义链定案（含 D20 拍数实测）；§1/§2 原文保留不动。七份探测档案见 `research/refs/phase2-消息注入/2026-09-22-戊探<A-G>-*.md`（research/ 不入库，本地留档），键序大词典同日修订版为准。
> **v3（2026-09-22 用户人工实测后验，效力最高）**：§6.3 七项争议全部裁决收敛、§6.4 用户实测键序补全（kimi Ctrl+S=立即插队、kimi 排队制定案、claude 撤回=功能、codex Tab 队列+Esc 直插、opencode Esc 直插+Ctrl+C 禁注）——**与探测结论不一致处，一律以用户实测为准**。

## §1 新 bug 根因（本轮实测）

| # | 现象 | 根因 | 定性 |
|---|---|---|---|
| N1 | codex 多题（Question 1/2）只显示题干列表，无选项可点，用户卡死 | 多题卡 v1 只读（批次丁 §2.3 边界：键序未定案不出手）。**但键序其实已定案**——批次丙复审探测 A 项 4/4 实证：数字即选即交→自动推进下一题→←/→ 切题（已答高亮保留）→末题 `enter to submit all` 一次提交全部、无确认屏。定案素材齐备却未接上交互 | 设计边界不再成立，必须升级 |
| N2 | codex 模式组「默认（不可用）」；权限组「模式未知」；点权限档报「菜单档位表不自洽（混入了正文行或被遮挡）——命令已发送，档位未切」 | 三层：①**权限组回读未校准**（codex 权限档底栏词表缺，Default 档靠"缺席推断"未实现 → 恒「模式未知」）；②**权限菜单解析怕叠加**：有待决问答框时点权限档，/permissions 菜单叠在问答框上 → 屏读见混合内容 → 簇判据「混入正文行」拒绝（审计 20:02 三连失败实证；20:00:46 无对话框时切「默认」成功过一次=机制本身通）；③**守卫时序缺口**：对话框在场时点击权限档，预注入守卫未拦住（命令已发送才在解析层失败）——模式组的「默认（不可用）」置灰疑似同一守卫的前置标注，但 UX 只说「不可用」不说为什么 | N2=守卫前置+解析加固+回读校准三件套 |
| N3 | 审批窗（红）与问答窗（蓝）风格不一致 | InteractiveCard tone 按卡片类型分色（审批=rose、问答=sky）；用户裁定**统一蓝色系**——审批对话框本质也是"等用户选择"，与问答同族 | UI 决策，成本低 |
| N4 | opencode 多题卡只读同 N1（4 题 tab 切换+Confirm 页） | 同 N1。opencode 键序素材同样已齐（本轮截图 footer 自证：`⇆tab ↑↓ select enter toggle esc dismiss` + Confirm 页；**注意多选模式下 enter=toggle 非提交**，Confirm 页才提交） | 同 N1 升级 |
| N5 | claude 审批卡超长侵占打字框 + 选项误纳 | 对话框屏读解析在「计划正文自带连续编号列表 + 真对话框选项在可视窗边缘」时选错簇（把计划验证步骤当选项）；且卡体无总高度上限（正文全量入卡），把 composer 顶出首屏 | 解析锚点+布局契约 |
| N6 | kimi 权限组「总是询问」报「档位表不自洽」 | kimi 三档中「总是询问」无直达斜杠命令（/yolo、/auto 只覆盖两档），走 /permission 菜单+定位 → kimi 菜单每档带**长描述折行** → 簇判据「混入正文行」失败；失败后确认键落在当前预选行 → no-op（审计 19:21:44/52/55、19:48:26 三连失败实证；按需/永不两档预选直达所以好用） | 菜单折行解析（与 N2 同构） |
| N7 | kimi 多选题点选项→按钮消失/只切行不勾选 | kimi 问答档 multiSelect 键序未实现：现 TwoPhaseSelect=[数字,回车] 在多选框里=**双切抵消**（footer 自证 `1-5 / ↵ toggle` 都是切勾）；正确键序=↑↓ 定位→数字/回车切勾（单次）→全勾完→tab 切 Submit 页→↵ 确认。KIMI3 实证：序列走到了 Submit 页且显示 Not answered | 批次丙复审 Important-1 现实爆雷 |
| N8 | kimi/codex/claude 模式栏不回显当前档 | 三家回读词表未校准（批次丙开放问题 3 遗留；素材已在独立探测档案：claude 环序屏显、codex Default=缺席推断、kimi plan 前缀+Never Ask 词） | 丁T4 遗留收尾 |

## §2 能力差距矩阵（R2 后现状 → 批次戊目标）

图例：✅=远程可用（实测）｜🟡=部分可用/有条件｜❌=不可用（用户会被卡死）｜➖=该工具无此机制

### 审批类
| 能力 | claude | codex | kimi | opencode |
|---|---|---|---|---|
| 审批二元卡（允许/拒绝） | ✅ | ✅ | ✅（导航确认，Reject 必达） | ➖（无审批映射；question 工具为主） |
| 审批 N 选项对话框（计划批准/Implement 等） | 🟡→解析锚点改造（N5） | ✅（数字直选） | ✅（导航确认） | ➖ |
| 计划批准卡聚合计划全文 | ✅ | ✅ | 🟡（文件卡+正文卡双卡已上） | ➖ |
| 审批卡配色统一蓝系 | ❌（N3） | ❌（N3） | ❌（N3） | ❌（N3） |

### 问答类
| 能力 | claude | codex | kimi | opencode |
|---|---|---|---|---|
| 问答单选（点选即答） | ✅ | ✅ | 🟡（单选人工实测✓；TwoPhaseSelect 数字可靠性待定案） | ✅ |
| 问答多选（勾选+提交闭环） | ✅（人工实测全链过） | ➖（request_user_input 官方单选为主） | ❌（N7 双切抵消；正确序=↑↓定位+切勾(数字/回车/空格待对照)+tab Submit+↵） | ❌（enter=toggle 语义待实机复核，**用户提出空格可能才是选中键——对照法：A 空格选、B 回车选，Confirm 屏读裁决**） |
| 问答多题（逐题+导航+终态提交） | 🟡（分页 ←→ 未测→只读） | ❌（N1 只读卡卡死；键序已探明） | ❌（多题只读） | ❌（N4 只读卡卡死；键序素材齐） |
| 自由作答（Type something/notes/Other） | ✅（卡内+composer=R2 必修1） | ❌（Tab notes 未探 → issue #78） | ❌（Other/Revise 未探 → issue #78） | ❌（own answer 未探 → issue #78） |

### 模式/权限类
| 能力 | claude | codex | kimi | opencode |
|---|---|---|---|---|
| 模式切换 | ✅（shift+tab 单轴） | 🟡（计划✓；默认恒标不可用待查 N2③） | ✅（/plan on\|off 直达） | ✅（shift+tab 单轴） |
| 权限档切换 | ➖（含在单轴内） | 🟡（无对话框时✓；问答待决时菜单解析失败 N2②） | 🟡（按需/永不✓；总是询问❌ N6） | ➖（配置文件域，会话内不可切） |
| 当前档回读/高亮 | ❌（N8 词表未校准） | 🟡（模式组✓；权限组❌） | 🟡（plan 前缀✓；权限档❌） | ✅（Build/Plan，好评基准） |

### 其他
| 能力 | claude | codex | kimi | opencode |
|---|---|---|---|---|
| 打断式插队 | ✅（R2 改屏读等回合已停+稳定闸，聚焦复审中） | ❌（底栏 esc interrupt 存在，未实现→批次戊候选） | 🟡（**用户记忆：busy 对话中 Ctrl+S 可插队——待探**，探明即实现；busy 裸注入丢消息=已知限制） | ➖ |
| 计划展示 | ✅ 正文+文件双卡 | ✅ 正文卡（markdown 待补丁T5） | ✅ 正文+文件双卡 | ➖（计划即普通消息） |

## §3 批次戊任务输入（按"用户不被卡死"优先级排序）

| # | 任务 | 覆盖差距 | 操作序列（已探明部分） |
|---|---|---|---|
| 戊1 | **codex 多题问答卡升级**（N1，卡死级） | 问答多题 | 数字即选即交→自动推进→←/→ 切题→末题 enter 提交全部（复审探测 A 项定案）；多题卡渲染逐题选项 |
| 戊2 | **opencode 多题/多选问答卡升级**（N4，卡死级） | 问答多选+多题 | ⇆tab 切题→↑↓ 定位→enter toggle 勾选→全题答完⇆tab 到 Confirm 页→enter 确认（本轮截图 footer 自证）；注意多选模式 enter=toggle 非 submit |
| 戊3 | **kimi 问答多选键序**（N7） | 问答多选 | ↑↓ 定位→数字或回车切勾（单次）→tab 切 Submit→↵ 确认；实机定案后实现 |
| 戊4 | **对话框选项解析锚点**（N5）：选项簇定位改「离输入行/底栏最近的合格簇」，弃最长簇；卡体总高度上限+正文默认折叠；**布局契约：任何浮窗不得侵占打字框** | N5 | — |
| 戊5 | **权限菜单解析加固**（N2②/N6）：菜单块支持折行描述归属；或改相对步进（从当前档回读推算位置→↑↓ 步进）；**待决问答在场时权限/模式按钮前置禁用+原因透出**（守卫前置，替代"命令已发送才失败"） | N2/N6 | codex /permissions picker、kimi /permission 菜单 |
| 戊6 | **三家回读词表校准+档位高亮**（N8，裁5 收尾）：claude 环序屏显词、codex Default 缺席推断+权限档词、kimi plan/Never Ask 词（漂移口径：批准后自动切出 plan）；模式栏当前档高亮 | N8/N2① | 屏读底栏文本（素材：独立探测档案 18 快照） |
| 戊7 | **审批/问答卡配色统一蓝系**（N3）：审批对话框卡（等用户选择）与问答卡同族 sky 色，二元审批红系保留或并入 | N3 | — |
| 戊8 | codex Default 模式「不可用」判定复核（N2③）：审计显示 20:00:46 切默认成功过、20:02 起恒不可用——确认是「待决对话框守卫前置标注」还是判定 bug；标注必须透出原因 | N2③ | — |
| 戊9 | 收口：门禁+清单+台账 | — | — |

## §4 终端文本采集能力（回答"能不能像 playwright 一样打印终端文本"）

**能，且已经建成**：批次丁已有的 `read_screen_window`（后端 Rust）与 `ConOut.ps1`（探测套件）就是终端可见窗口的**逐行文本打印**——独立复审探测的 91 项证据里每一步都附了屏读文本（如 `screen-r1v-after-down.txt` = 按下 ↓ 后的逐行原文）。

**从现在起的证据规范**（写进批次戊执行提示词）：所有探测/修复验证的每一步必须附「注入了什么键 → 屏读逐行原文」对照，不再只给截图；你只需要在行为怪异时告诉我们**大致时间点**，我们从审计表（每次注入的键/结果都有落账）+ 屏读重放直接定位，不需要你手抄终端内容。

## §5 本轮已消失/已正常项（免重复报告）

- opencode 模式回读「计划」正常（你点评的基准项）；
- opencode 问答卡已从红卡变蓝卡（批次丁后问答走问答卡路径）；
- claude 幽灵审批标记、kimi 单选即答、claude 多选勾选+Review 闭环——均保持正常。

## §6 Stage 1 探测定案回填（2026-09-22，两段式第一段收口）

> 探测执行：4 个探测子代理按终端分组并行（A=opencode；B+D-kimi+F-kimi=kimi；C+D-codex+F-codex=codex；E+F-claude+G=claude），主控合并；全部自 spawn 会话、八条铁律、清场复核零存活。
> 证据规范=裁15（每步「注入键→屏读逐行原文」对照表）。档案 7 份：戊探A（opencode 多选/多题）、戊探B（kimi 问答）、戊探C（codex Tab 备注）、戊探D（权限菜单底料，codex+kimi）、戊探E（claude 对话框锚点）、戊探F（插队底料，三工具）、戊探G（D20 拍数）。

### 6.1 N1–N8 定案状态

| # | 根因 | Stage 1 探测定案 | 档案 | Stage 2 输入变化 |
|---|---|---|---|---|
| N1 | codex 多题卡只读 | **键序全链定案**：数字即选即交→自动推进；Tab=就地备注行 `› Add notes`；备注态 ←/→ 不切题、备注态 Enter=提交当前高亮项+备注（无未答确认屏）；多题=每题 `[Tab→备注→Enter]`、末题 Enter submit all；落账 `user_note`=answers 数组第二元素、各挂各 question_id | 戊探C | 戊1 键序档可直接实现，备注键序一并纳入 |
| N2 | codex 权限组三件套 | 菜单底料齐：4 项+折行不产新项+**底锚定**（4 dump 行号级一致）+短语锚最稳；**busy 中菜单照常打开**（官方「运行中不可用」UI 层不成立 ×2）、菜单内 Esc=关菜单不中断；**问答待决叠加=菜单打不开**（斜杠文本被静默吞 + Enter 被吃=提交默认答案 ×3）；「档位表不自洽」混屏形态在原生注入层未复现（留对账） | 戊探D codex 段 | 戊5 解析改「锚点+两行组」；**戊8 守卫前置升级为硬需求** |
| N3 | 配色 | 无探测面（纯 UI） | — | 维持裁11 |
| N4 | opencode 多题/多选卡只读 | **键序全链定案**：VK tab 前向循环切页（新发现 h/l 切页）；↑↓ 定位（VK/VT 双形态，属性 0x0003）；**enter=真 toggle（×3）；空格 char/VK 两形态均无效（×3，用户猜测不成立）**；数字=toggle；own answer=↓定位→enter 开输入行→打字→enter（裸打字会被当导航/勾选）；Confirm enter 一次提交；esc 取消=`error`+dismissed；落账 answers **二维数组按题序**+`multiple` 字段；勾选态属性可检（0x000B/0x000F） | 戊探A | 戊2 键序档定案；toggle 判据可走属性读 |
| N5 | claude 审批卡误纳+超长 | 误纳几何复现（正文编号簇与真选项簇同屏、隔滚动提示/分隔/标题行）；**「离底栏最近合格簇优先」2/2 选中真簇；标题行 `Claude has written up a plan…` 逐字节稳定**；误纳夹具固化（30 行逐行原文）；数字键复验 ×5 无效果（裁决加固）；卡体高度契约无探测面 | 戊探E | 戊4 解析锚点测试夹具已备 |
| N6 | kimi「总是询问」定位失败 | 精确触发行锁定：`Never interrupts you; …` 描述行含 `Never` 子串→`Never Ask` 重复命中；**五锚充分（标题行/footer/`← current`/`▶`/两行组）→簇判据可退役** | 戊探D kimi 段 | 戊5 同 N2 方案；kimi 权限档回读可直读 `← current`（N8 素材） |
| N7 | kimi 多选双切抵消 | **键序定案**：↑↓定位→切勾单次（**数字/回车/空格三通道全成立**）→tab 一次直达 Submit→确认键 char '1'/VK '1'/VK Enter 全通（×7）；Other 全链 ✓；落账 `interaction.resolved`+method 定案；单选数字可靠性收口（char/VK 各 3/3 直达 Review，R1-I1 关闭） | 戊探B | 戊3 键序档定案；TwoPhaseSelect 争议收口 |
| N8 | 三家回读词表未校准 | 本轮未直接产出三家居中态词表，但新增权限档回读源：kimi `← current` 后缀+`▶` 预选直读；codex 菜单 `(current)` 后缀+底锚定 | 戊探D | 戊6 词表素材扩充 |

### 6.2 插队（「其他·打断式插队」行）定案

| 工具 | 定案 | 档案 |
|---|---|---|
| claude | 丁T9 链 2.1.278 复现（队列不丢、Esc 取出开新回合）；新增：队列 hint `ctrl+x ctrl+s to send now`（未测）；**早 Esc 边界**——提交后 ≤~8s 无输出窗口内 Esc=取消发送（消息退回输入行），~13s 后=正常中断（2 取样，边界未定） | 戊探F claude 段 |
| codex | busy 打字=草稿（footer `tab to queue message`）；**单次 Esc=立即中断（无确认弹窗）+草稿保留，需手动 Enter 提交**；落账 `<turn_aborted>`+草稿 user message；Tab 原生排队通道未测（补测价值最高） | 戊探F codex 段 |
| kimi | **排队制不丢**：回合结束 50–86ms 自动开新回合（×2 wire 对账）——修订词典「busy=直接丢失」；Ctrl+S 注入 ×4 零效〔矛盾〕待人工键盘复验 | 戊探F kimi 段 |
| D20 拍数（执行者遗留） | Esc→单帧停 ≈250/269ms；**稳定判据主读数 ≈353/374ms**；0/3 瞬态复发 → **建议 `TURN_STOP_POLL_TOTAL_MS` 维持 3000、`TURN_STOP_STABLE_FRAMES` 维持 2、钉值测试不动**（仅探针文档补实测记录）——只写建议未改代码 | 戊探G |

### 6.3 争议裁决（2026-09-22 用户人工实测后收敛，与探测结论不一致处以用户实测为准）

| # | 原争议/未定案 | 裁决 |
|---|---|---|
| 1 | kimi Ctrl+S：官方 tip vs 注入零效 ×4 | **裁决：功能存在**——用户真键盘实测 Ctrl+S=排队中**立即插队**；注入无效归因注入事件形态/探测通道（Stage 2 用产品 Rust 引擎通道复验，不阻塞语义定案） |
| 2 | kimi busy「丢失 vs 排队」 | **裁决：排队制定案**——用户实测与戊探F wire 对账一致，丁复审「丢失」作废；队列态语义（用户实测）：Ctrl+S=立即插队、↑=再编辑排队消息、Ctrl+C=取消正在编辑的消息 |
| 3 | codex N2「档位表不自洽」混屏 | 实现链路归因项（非用户测试项）：系 MAM 在问答待决时点权限档、菜单与弹窗内容混屏致屏读解析拒绝的旧报错；探针已证原生层该态菜单根本打不开，**守卫前置落地后该路径不可达**——留 Stage 2 回归确认即可 |
| 4 | claude 早 Esc「取消发送」窗口边界 | **裁决：撤回是功能非异常**——用户实测：已发出消息 Esc=撤回回编辑态（可继续编辑）、编辑界面 Esc×2=取消编辑内容；实现按「Esc 一次+屏读区分『撤回（消息回输入行）』vs『中断（`Interrupted` 标记）』」处理，无需时间阈值 |
| 5 | codex Tab 原生排队 | **用户实测补全（键序定案）**：busy 打字→Tab=入队；队列 Esc=打断前台+直插（**codex 插队键序：打字→Tab→Esc×1**）；Ctrl+C=撤回队列消息（打断前台回合，用户裁决可接受）、撤回态 Ctrl+C=取消编辑 |
| 6 | opencode busy 插队语义 | **用户实测**：Esc=打断前台+直插 ✓；队列消息不能撤回继续编辑；⛔ **Ctrl+C=直接退出 opencode——一律禁注** |
| 7 | kimi 权限菜单 busy 态叠加 / 工具批准框叠加 | 用户指出批准框态≠busy（该组合对 MAM 不可达：对话框在场守卫拦截）→ 降级为低价值留白，不阻塞 |

### 6.4 用户人工实测键序补全（2026-09-22，效力最高）

| 工具 | 键序行（状态 → 键 → 效果） |
|---|---|
| kimi | 队列态：**Ctrl+S=立即插队**；↑=再编辑排队消息；Ctrl+C=取消正在编辑的消息 |
| claude | 队列态 Esc=打断+插队（取出开新回合）；已发出消息 Esc=**撤回回编辑态**；编辑界面 Esc×2=取消编辑内容 |
| codex | busy 打字→**Tab=入队**；队列态 **Esc=打断前台+直插**；Ctrl+C=撤回队列消息（打断前台回合，可接受）；撤回态 Ctrl+C=取消编辑 |
| opencode | busy **Esc=打断+直插**；队列不可撤回编辑；⛔ Ctrl+C=退出应用（禁注） |

**用户复测第二轮（2026-09-22 晚，16 项留白清单，效力最高）**：
- **CL-3 终裁（影响戊5）**：claude 单选题**数字直接选中**（推翻探测 ×5 零效果；归因候选=注入早于选项渲染）→ 引擎新增通用规则「**注入前屏读确认选项簇已渲染**」；claude 审批键序改「**数字优先+屏读验证+导航回退**」双路径。
- **K-1（N6 收口+N8 定案，影响戊5/戊6）**：kimi 菜单 **↓ 每按恰移一档**（跨折行不偏）——N6 失败全在 MAM 解析、TUI 无问题；**切档回执 `Permission mode: <档名>`+描述行**=kimi 权限档回读锚（三档词形实录）。
- **K-5（影响戊3）**：kimi 多题问答=题屏数字**直选+自动推进下一题**+Other 按编号打字+**终态 Review 汇总屏**（`Ready to submit your answers?` [1]/[2]）——与 codex（数字+自动推进）/claude（Review 屏）**同构**，四家多题形态统一。
- **CX-1/2+K-2（影响戊8 守卫）**：codex busy 中换档**直接生效不打断**（Full Access 二段确认照弹）；kimi busy 中 `/permission` 可切不打断——**busy 态一律不拦权限/模式**（「不可用」置灰仅限问答待决态）。
- **CX-3/5（codex 键序补全）**：↑=复制最新消息进输入框；Ctrl+C=打断+内容回输入框（撤回编辑）；队列 Esc=插队、**再 Esc=打断会话且编辑框空**；弹窗 Tab=选项↔文本 toggle（框内数字入文本）、选项模式数字直选再确认、弹窗 Esc=中断整个会话。
- **CL-1/2（claude 补全）**：多条队列 **Esc×1=中断+整队批量取出开新回合，再 Esc=整队批量撤回回编辑**（澄清定案，与 kimi Ctrl+S 批量语义对称——队列冲刷=整队非逐条）；`ctrl+x ctrl+s` hint 真键盘亦不生效（插队仍走 Esc）。
- **K-3/K-4（kimi 补全）**：多条排队 Ctrl+S=**一次性批量全部进入**；Esc 后**排队条目立刻被发给模型开始新回合**（澄清定案=打断版插队：中断使回合提前结束→触发既有自动提交）——kimi 插队两路 Ctrl+S（不打断）/Esc（打断版）。

**剩余开放项（全部为实现侧工作，无需用户再测）**：① kimi Ctrl+S **注入形态**（产品 Rust 引擎通道复验——PS 探测通道 ×4 无效不等于产品通道无效）；② codex N2 守卫前置落地后的回归确认；③ 低价值留白：claude 多条队列取出顺序与 `ctrl+x ctrl+s` 直发键、kimi 队列 `↑`/Ctrl+C 的注入驱动键（VK 常规形态，风险低）。
> 留白项已出**用户实测清单**（`research/refs/phase2-消息注入/2026-09-22-戊探留白用户实测清单.md`，16 项分组逐步骤）——用户测、主控记，按「用户实测效力最高」回填。

### 6.5 守卫设计原则（2026-09-22 用户裁决：错误拦截会让功能不可达）

**判定标准 = 点一下是「无效果」还是「破坏性副作用」**。点了没效果的不拦（白点一次+回执如实报「未生效+原因」）；点了会破坏状态的才拦（置灰+原因透出）。

**必须拦（点≠无效果，点=破坏，实证）**：
1. **问答/弹窗待决时点权限档或模式切换**——codex：Enter 被弹窗吃=**提交默认答案**（问题被乱答，×3）；kimi：Enter=**误选高亮项推进 Review**（待决态被污染，×2）。这不是「不生效」，是「把挂着的问题答掉」→ 置灰+原因（N2③）。
2. **opencode Ctrl+C**（=退出应用）→ 任何状态一律禁注。
3. **kimi 审批框数字通道**（'2'+Enter 误批准实证，R1-I1）→ 禁用。
4. **claude 问答数字已提交后补 Esc**（Esc 落入 busy=中断模型回合，K2）→ 序列层禁止。
5. **opencode 自由作答未确认输入行开启前不发文本**（裸打字被当导航/勾选指令：'l' 切页、'2' 乱勾）→ 时序闸：屏读确认输入行已开才打字（时序守卫，非按钮禁用）。

**不拦（看似危险，实测可用——拦了就不可达）**：codex busy 中切权限（官方「运行中不可用」实测不成立，勿按官方文档拦）；四家 busy 中的排队/插队全家（排队制+插队键均用户确认可用）；队列消息的编辑/撤回各键（↑/Ctrl+C/Ctrl+S/Esc 直插）。

**状态不确定时**：注入前单次屏读快照（D20 例外条款允许）判「破坏态」→ 拦+透出原因；判不清 → 放行 + 注入后屏读验证 + 如实回执（失败明说失败，绝不假成功）。

### 6.6 实测 × 现行实现矛盾审计（2026-09-22；Stage 2 执行序：**先改 A 类矛盾，再补 B 类新行为**）

> 对照对象：现行产品代码 baked-in 的键序决策（`inject/approve.rs` 键序档 / `inject/question.rs` 键配置 / `inject/queue.rs` 插队投递路径 / `inject/mode.rs` 菜单定位）+ 验收清单 C-4/C-23/C-25/C-34、R1-1/R1-2、H-4 快照 × 三轮用户实测。§2 现状矩阵为历史基线快照不改，矛盾与修正统一落本节。

**A 类 · 矛盾必修（按危害排序）**：

| # | 矛盾 | 现行实现依据 | 用户实测 | 修法（Stage 2 落点） |
|---|---|---|---|---|
| A1 | **插队投递的「撤回窗口拼接」危害（唯一现行代码级危害）** | `queue.rs interrupt_first`：Esc → 屏读等回合停（busy 串消失×2 拍）→ **直接打字+回车**；判据无法区分「中断停」vs「撤回停」 | Esc 落在已提交未输出相位=**撤回，旧消息回到输入行**（不清空——C-25 留白「Esc 是否清输入行」已答：不清）→ 此时追加打字=**旧消息+新消息拼接双发** | 投递前屏读检查输入行**非空即中止+如实回执**（或探明清空键后清空再发，清空键未实测——先做检测中止，保守）；列入戊8 首项 |
| A2 | **claude 审批键序 NavigateConfirm 唯一路径**（次优+过度依赖屏读高亮解析） | R1-2/C-23/E-6③ 按「计划批准框数字无效」（丁复审 ×3+戊探E ×5）baked `ApproveDialogKeys::NavigateConfirm` | CL-3 终裁：**数字直接选中**（归因候选=注入早于选项渲染） | claude 改「**数字优先+屏读验证+导航回退**」双路径；引擎通用规则=注入前屏读确认选项簇已渲染。**注意：kimi 审批框数字禁令维持**（R1-1 误批准证据未被推翻——K-5 测的是问答屏非 `Ready to build` 审批框，两框不同族不得混改）；列入戊5 |
| A3 | **kimi TwoPhaseSelect 不得复用到多题**（潜在 bug） | `question.rs` kimi=TwoPhaseSelect `[数字,Enter]`；单题恰好闭环（数字→Review 屏→Enter=确认 Submit）；H-4④③「数字可靠性未独立实测」 | K-5：多题=数字**直选+自动推进下一题**——尾随 Enter 会作用在**下一题**（误选/误推进）；戊探B+K-5 已消除 H-4 的未测申报 | 戊3 接 kimi 多题时**必须**用「数字+自动推进、不加尾 Enter」；单题 TwoPhaseSelect 维持；写入戊3 任务约束 |
| A4 | **kimi 权限菜单「总是询问」定位失败**（用户已报 N6，现行 bug） | `mode.rs locate_menu_items` 关键词包含判据：`Never interrupts you;…` 描述行重复命中 `Never` → 不自洽拒绝（N6 审计三连失败） | K-1：TUI **↓ 每按恰移一档**（跨折行不偏）——失败全在 MAM 解析，非 TUI | 戊5 本体：改两行组锚（标题↔footer 窗+标签/描述成组+`← current`/`▶` 读态），簇判据退役 |
| A5 | **codex 模式组「默认（不可用）」置灰根因待核** | N2③：20:00:46 切默认成功、20:02 起恒不可用——置灰条件疑似含 busy 态（误伤）或回读失败 | CX-1/2：busy 中换档**直接生效不打断**（Full Access 二段确认照弹） | 戊6/戊8：核实守卫条件只拦「对话框在场」不拦 busy；Default 档回读=缺席推断补实现（N2①）；busy 一律不拦已成文 §6.5 |
| A6 | 文档口径残留（低优先） | C-4「busy 插队命中排空回执」为批次乙旧口径（R2 已改屏读等回合停，C-25/C-47 为新口径） | — | Stage 2 收口（丁T6'）时统一清单新旧口径 |

**B 类 · 新增行为（无现行实现冲突，按 §4 任务序补入）**：kimi 插队两路（Ctrl+S 不打断优先/Esc 打断版）+排队制回执（戊8）；codex 插队改用户键序=打字→Tab 入队→Esc 直插（探针「草稿+Esc+手动 Enter」为回退路径）（戊8）；opencode 插队=Esc 打断直插+⛔Ctrl+C 禁注（戊8）；kimi 多题卡=数字+自动推进+Review 汇总（与 codex/claude 同构）（戊3）；codex Tab 备注/自由文本 toggle+框内数字入文本（issue #78，戊4）；kimi 权限档回读锚 `Permission mode: <档名>`+codex `(current)`（戊6）；claude/kimi 队列整队批量语义（回执按整队设计）（戊8）；busy 态一律不拦权限/模式（守卫条件收窄，§6.5）。

> **§6.6 落点映射（2026-09-22 补）**：修法落点已按 Stage 2 计划编号对齐（`2026-09-22-phase2-batch-e-stage2-plan.md`）——A1→S2-T1、A2→S2-T2、A3→S2-T6（任务约束）、A4→S2-T3、A5→S2-T4、A6→S2-T13；B 类→S2-T6–T12（T10 含 opencode 插队扩入，设计文稿 §1 已同步）。

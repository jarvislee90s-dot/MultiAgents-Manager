# 批次戊 · 远程端交互全能力闭环 · 设计文稿与探测操作计划（两段式）

> **For agentic workers:** 本文档是批次戊的**设计定案 + Stage 1 探测操作计划**。**Stage 2（编码+TDD 全流程计划）在 Stage 1 探测收口后另行产出**——两段严格分离：探测阶段不改任何产品代码；编码计划以探测定案表为输入，未定案的键序不得进编码。执行方式：subagent-driven-development，评审按用户指令为**单评审员一轮**（规格+质量合并）。
> **依据档案：** 批次丁 gap 矩阵 `2026-09-22-phase2-batch-e-gap-matrix.md`（§1 八个根因/§2 现状矩阵/§3 任务输入）+ 键序大词典 `research/refs/phase2-消息注入/2026-09-22-键序大词典.md` + issue #78（三家自由作答）+ 三家官方文档调研。
> **红线：** 不 push、不动 main、不动宪法；探测阶段零产品代码改动；编码阶段六门禁全绿（CARGO_TARGET_DIR=target/gate-run、bin 须 --features hook-listener、vitest 终跑勿与 cargo 并行）；实机探测 #[ignore] 或探测脚本、遵守八条铁律；台账如实；探测与编码各自独立 commit。

## §0 用户裁决清单（累积，刚性）

批次丁 §0 裁1–裁10 全部延续（计划双卡/签名后置/自由文本不误触/多选两段+闭环/切后必知/opencode 改默认/legacy 标注/屏读出卡/对话框在场红线/kimi server 延后），本轮新增：

| # | 裁决 | 精确含义 |
|---|---|---|
| 裁11 | **配色统一蓝系** | 审批对话框卡（N 选项）与问答卡同族蓝系（都是「等用户选择」语义）；二元审批（允许/拒绝、y/esc 类）保留红系警示或并入蓝系，随 UI 方案定 |
| 裁12 | **浮窗不侵占打字框（布局契约）** | 任何卡片/浮窗不得把 composer 顶出首屏或遮挡输入框——卡体设总高度上限，长内容默认折叠+点开 |
| 裁13 | **多题/多选卡必须可交互** | 远程端=桌面端全部能力：用户不得因卡片只读而卡死（codex 多题、opencode 多题/多选本轮已实际卡死用户） |
| 裁14 | **两段式流程** | 探测（不改代码）前置并穷尽→定案表回填词典/矩阵→编码+TDD 计划另行产出后开工；探测与编码不混批 |
| 裁15 | **证据规范** | 一切实机验证每步附「注入了什么键 → 屏读逐行原文」对照；用户只审报告/词典，不需手抄终端 |
| 裁16 | **队列整队批量** | claude/kimi 队列冲刷=整队批量（取出/插队/撤回均作用于全部条目），回执按整队设计（用户实测 2026-09-22） |
| 裁17 | **busy 态不拦** | 权限/模式切换在 busy 态可用且直接生效（codex 含 Full Access 二段确认、kimi 同）——守卫只拦「问答/弹窗待决」（用户实测） |
| 裁18 | **数字优先+渲染等待** | claude 审批框数字直选有效（推翻探测「无效」）；注入前屏读确认选项簇已渲染；键序=数字优先+屏读验证+导航回退。kimi 审批框数字禁令维持 |
| 裁19 | **Ctrl+C 禁注** | opencode 一律禁注（直接退出应用）；codex 仅「撤回」语义可用（副作用=打断前台，用户已接受） |
| 裁20 | **词典单一口径** | 被用户实测推翻的旧结论在键序大词典中**直接删除**，一行只留一种表述（防执行 agent 误读）；历史对账只进台账/探测档案 |

## §1 范围

**入**：四家（claude/codex/kimi/opencode）的 问答多题/多选交互、三家自由作答（issue #78）、对话框解析锚点、权限菜单解析加固、回读校准+档位高亮、打断式插队扩展（codex/kimi；**opencode 于 Stage 1 用户实测后扩入**——Esc 打断直插 + Ctrl+C 禁注，裁19）、配色统一、composer 占位一致性。
**不入**（维持批次丁 §5 与本轮追加）：kimi server API（未开）、opencode 会话内权限切档（官方无能力）、codex Permission Profiles（beta）、协议路线（三期）、zcode/dsh/workbuddy/openclaw。

## §2 现状基线

> **注（2026-09-22 Stage 1 收口后）**：本节为 R2 时点快照，保留作历史基线；Stage 1 探测 + 三轮用户实测后的现状、定案与矛盾审计以差距矩阵 §6 为准。

R2 后（HEAD 3c0929b）：四家「问答单选即答/审批二元/审批 N 选项（解析锚点待改）/模式切换+模式组回读/计划卡」可用；**缺口**=问答多选（kimi 双切抵消、opencode toggle 未实现）、问答多题（四家只读或缺失）、三家自由作答、权限菜单定位（kimi 总是询问/codex 有待决时）、权限档回读（codex/kimi）、打断式插队扩展（codex/kimi）、对话框解析误纳（claude 计划正文编号列表误纳实证）、配色与布局（裁11/12）。详见 gap 矩阵 §2。

## §3 Stage 1 · 探测操作计划（编码前完成，全部不改产品代码）

**通用纪律**：八条铁律（只碰自 spawn 进程、临时目录带 run-id、日志追加、每键记 BOOL/实写数、结束清场、结论不超证据）；**每步附「注入键 → 屏读逐行原文」对照**（裁15）；≥2 取样、争议 ≥5；探测产出=定案表（回填键序大词典+gap 矩阵 v2）。可并行分组执行；预计总量 2-3 个探测工作日。

### 探测 A · opencode 多选/多题键序（供戊2）
- 触发：question 工具多选题（可多选，≥3 选项 + own answer；再多触发一组多题）。
- 定案项：①`⇆tab` 切题与 Confirm 页；②`↑↓` 定位；③**切勾键对照法**——选项甲用空格、选项乙用回车，进 Confirm 页屏读裁决谁是真 toggle；④Confirm 页提交键；⑤"Type your own answer" 选中后打字→提交全链；⑥多题答完 Confirm 的落账形态（SQLite answers）。
- 产出：opencode 问答键序定案表（单选/多选/多题/自由作答四行）。

### 探测 B · kimi 问答键序定案（供戊3，含 R1-I1 收口）
- 触发：question 单选与 `multi_select:true` 各若干。
- 定案项：①单选数字可靠性复核（TwoPhaseSelect 争议）；②多选切勾键对照（数字/回车/空格——footer 称 `1-5 / ↵ toggle`，用户提空格，裁决）；③tab→Submit 页→确认键；④Other 自由作答全链；⑤Revise 反馈子流（`Type feedback · ↵ submit` 与「追问+Review」子流）对文字路径的影响；⑥答案落账核对（wire resolved 按题干键控+method）。

### 探测 C · codex Tab 备注全链（供戊4=issue #78）
- 触发：request_user_input（题数 1 与 2 各一）。
- 定案项：①Tab 后备注编辑形态（就地/独立框）；②打字通道（B 族 VK）与长度上限；③备注与选项选择的关系（只备注不选？）；④提交后 rollout `user_note` 落账；⑤多题时备注归属。

### 探测 D · 权限菜单屏读底料（供戊6）
- 触发：codex `/permissions`（三态：空闲/问答待决叠加/运行中）与 kimi `/permission`。
- 定案项：①菜单逐行原文（含折行描述归属）；②「不自洽」失败的精确触发形态；③有无稳定锚（标题行/编号/位置）可替代簇判据；④待决问答叠加时的混合屏读样本。

### 探测 E · claude 对话框选项锚点底料（供戊5）
- 触发：计划批准框，且正文自带连续编号列表（复现本轮误纳场景）。
- 定案项：①真对话框选项簇与正文簇的屏读位置关系（离底栏距离/标题锚）；②锚点策略候选实测（最近底栏簇优先？对话框标题行锚？）；③误纳复现夹具固化。

### 探测 F · 打断式插队扩展底料（供戊8）
- codex：busy 中 esc 单次 → 中断确认 + 队列/草稿消息去向（对照 claude 丁T9 形态）；**单次 Esc，严禁多按**（用户裁定）。
- kimi：**Ctrl+S**（用户记忆可插队）→ 触发形态/菜单去向/与 busy 丢消息限制的关系。
- claude：复核现实现（R2 屏读等回合停+稳定闸）在长收尾（>3s）场景的行为样本。

### 探测 G · D20 拍数回填（执行者遗留自裁值收口）
- `cargo test --lib d20_turn_stop -- --ignored --nocapture`（conhost+MAM_D20_PROBE_PID）→ 取稳定判据实际拍数/窗宽 → 回填 `TURN_STOP_POLL_TOTAL_MS`/`TURN_STOP_STABLE_FRAMES`/`timing.rs` 钉值三处。

### 探测产出与流转
①键序大词典逐项更新（〔待测〕→〔实测〕）；②gap 矩阵 v2（定案列回填）；③**批次戊编码计划 v2**（Stage 2 文档：任务×定案表×TDD 测试清单×文件地图，含既有机制复用点）；④用户过目定案表 → Stage 2 开工。

**Stage 1 已收口（2026-09-22）**：七份探测档案（戊探A–G，`research/refs/phase2-消息注入/`）+ 三轮用户实测裁决（矩阵 §6.3/§6.4：Ctrl+S 插队/排队制/撤回=功能/Tab 队列/数字直选/N6 收口/多题同构）+ 守卫设计原则（§6.5）+ 实测×现行实现矛盾审计（§6.6：A1–A6 必修/B 类新增）+ 词典清淤（裁20 单一口径版）。④已完成——Stage 2 计划见 `2026-09-22-phase2-batch-e-stage2-plan.md`（本轮起任务编号以该文档 S2-T* 为准，废止本文与 gap 矩阵 §3 两套旧戊编号）。

## §4 Stage 2 · 编码计划（v2 定稿指针——全文见 `2026-09-22-phase2-batch-e-stage2-plan.md`）

执行序=**先改矛盾、再增行为**（矩阵 §6.6 审计结论）：第一波 S2-T1–T5 清 A 类矛盾（T1 插队撤回窗口防护为最高优先——唯一现行代码级危害），第二波 S2-T6–T12 补 B 类新能力，S2-T13 收口。任务详情（定案输入/文件地图/TDD 夹具/DoD）以 Stage 2 计划文档为准；旧编号映射亦在该文档 §0。TDD 纪律不变：探测定案表屏读原文做夹具（失败测试先行）→ 实现 → 端到端 `#[ignore]` → 单评审员一轮复审。

## §5 验收标准（细化已入 Stage 2 计划）

远程端四家对照矩阵（同 gap 矩阵 §2 格式）全 ✅；卡死场景清零（问答/审批任何状态下用户均有可用操作）；四家问答/审批卡同设计语言；模式切换后四家回显当前档；六门禁全绿；issue #78 关闭。逐任务 DoD 与收口清单 I 节的生成口径见 `2026-09-22-phase2-batch-e-stage2-plan.md` §验收。

## §6 流程

Stage 1：探测代理（并行 A/B/C 与 D/E/F/G）→ 探测档案 + 词典/矩阵回填 → **用户过目定案表** → 主控出 Stage 2 编码计划 → Stage 2：编码子代理+单评审员每任务 → 收口停下等主线 review。

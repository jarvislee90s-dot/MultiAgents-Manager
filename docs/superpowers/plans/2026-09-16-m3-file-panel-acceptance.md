# M3+ 会话文件面板 · 实施与验收记录

- 计划：`docs/superpowers/plans/2026-09-16-m3-file-panel.md`
- 分支：`feat/m3-board-realtime-content`（基线 `a6bf07c`，即 Bug 1–8 修复后）
- 日期：2026-09-16
- 范围：会话详情页新增「文件面板」——把会话工具调用涉及的文件聚合为可排序、
  可过滤、可分档追溯的列表，点击行内文件名进入既有文件预览

## 实施结果（Task 1–3）

| Task | Commit | 内容 |
|---|---|---|
| 1 | `77bd790` | 后端 `/session-files` 富化：`FileEntry{path,lastSeq,lastTs,hits}` 出现序 + `limit` 参数化 + `truncated` 透传 |
| 2 | `d69599f` | 前端 API 层（`SessionFileEntry` / 新 `fetchSessionFiles` 签名）+ SessionDetail 两视图状态机（`list`/`file` 辨识联合）+ 页头面板入口 + 档位 scope |
| 3 | `08ec575` | `FilePanel` 组件（列表 / chips 过滤 / 档位卡片 / 空态 / 加载态）+ `FilePreview` 返回列表按钮 + 集成接线 |

### 关键实现口径（对齐用户拍板决策）

1. **排序键 = 会话出现序**：后端按 `lastSeq` 降序输出，前端零重排（`lastTs` 仅供展示，`null` 显示 `—`）。
2. **窗口机制零新增**：`limit` 直接透传 `read_session_messages_impl`——条数窗、字节窗（512KB×⌈limit/200⌉ 封顶 4MB）与 `truncated` 全部复用主会话同一套管线（Bug 1 的产物），后端未新增任何预算代码。
3. **scope 独立三档 200/500/1000**：`truncated && scope < 1000` 显示「范围内还有更早文件」；1000 到顶不显示。
4. **方案 A 后端富化**：提取/排序/去重/计数全在后端（`files.rs` 单一数据源），前端纯渲染。
5. **chips 全部/文档/图片**：`fileKindOf` 判扩展名（文档 md/markdown/txt；图片 png/jpg/jpeg/gif/webp/svg/bmp）；代码文件仅在「全部」可见，不做代码专属档。
6. **列表行**：主行 = 末段文件名（加粗，即超链接）；次行 = 目录前缀 + 相对时间 + `N×` 徽标（`hits=1` 不显示）。
7. **归一化去重 v1**：比较键 = `\`→`/` + Windows 语义小写（平台参数注入，跨平台可测）；**展示保留最后一次出现的原始形态**。
8. **v1 不做**（v2 池）：图片缩略图、代码类型档、文件名搜索、读/写标记、磁盘 mtime 徽标。

### 附带口径

- 面板挂载复用详情页已有的提取结果（scope=200 那次拉取一份数据两用：正文链接化 Set 派生自 `fileEntries`），**切档才重拉**。
- 面板默认布局：宽屏（≥768px）`split-h`（左对话右列表）、窄屏 `fullscreen`；三态随时可切（复用 `PreviewModeSwitcher`）。
- 不可预览文件（cwd/主目录外、敏感目录）照列不预过滤，点击走既有 403 错误态。
- 预览来源区分：从面板进入显示「返回文件列表」按钮；从消息正文链接进入无按钮（既有行为不变）。

## 测试与门禁

| 门禁 | 结果 |
|---|---|
| `cargo test` | **624** lib + 4 + 5 集成，全绿 |
| `cargo clippy --all-targets -- -D warnings` | 0 错误 |
| `cargo fmt --check` | 干净 |
| `pnpm test` | **403**（61 文件），全绿 |
| `pnpm build:mobile` | 成功（dist-mobile 已用最终源码重建） |
| `pnpm format:check` / `pnpm lint` | 干净（eslint 仅 3 条 resources 组件既有 warning） |
| `pnpm check`（format+lint+build） | 成功 |

新增测试覆盖（Task 1–3 累计）：

- **Rust**：`FileEntry` 四字段（hits/lastSeq/lastTs/末次原始形态）；多文件出现序降序；归一化去重（Windows 合一取末次形态 / 非 Windows 不合并大小写）；`normalize_key` 纯函数矩阵；`lastTs=None` 容忍；camelCase 序列化契约；`limit` 透传端到端（小 limit 只见尾条文件、大 limit 早期文件回归）；端点 wire 契约（结构化条目 + truncated）。
- **前端**：`fileKindOf` 三档矩阵（含大写、无扩展名、Windows 分隔符）；渲染顺序 = entries 顺序；行信息齐全 / `hits=1` 无徽标 / `null` 占位；主行点击透传完整路径；chips 三档过滤与过滤空态；档位三态（提示仅在 `truncated && scope<1000`）；空态 / 加载态；面板 → 文件 → 返回列表（mode 保持）；正文链接进入无返回按钮。

## 手动验收清单（需用户执行 · 手机或浏览器）

参考真实 kimi 长会话；桌面端 `pnpm tauri:dev`，手机浏览器打开远程地址。

| # | 场景 | 预期 |
|---|---|---|
| 1 | 打开面板 | 点详情页页头「文件面板」图标 → 宽屏左对话右列表（split-h）/ 窄屏全屏；列表按最后出现倒序（新文件在上） |
| 2 | 行信息 | 主行文件名可点击进预览；次行显示目录前缀 + 相对时间 + `N×` 徽标（仅出现多次的文件有徽标） |
| 3 | 档位 | 切 500 / 1000 列表变全；`truncated` 且未到顶时列表尾出现「范围内还有更早文件，可扩大追溯范围」；1000 到顶无提示 |
| 4 | chips | 「文档」只显 md/txt，「图片」只显图片；代码文件仅在「全部」可见 |
| 5 | 预览往返 | 面板 → 点文件 → 预览页头出现「返回文件列表」→ 点返回回到列表且布局保持不变；从消息正文链接点开则**无**返回按钮 |
| 6 | 布局三态 | 面板与单文件预览共用页头切换器，上下 / 左右 / 全屏三态正常，分隔条可拖动 |
| 7 | 失败态 | 不可预览的文件（项目目录与主目录之外、或敏感目录内）点击 → 既有「无法预览该文件」 |
| 8 | 回归 | 消息流正文链接化、加载更早消息、总结模式折叠提示不受影响 |

## 风险与已知边界

- `extract_chain_*` 七工具链路用例按「出现序降序」修正了期望值（原为插入序）——属契约变更的预期结果，非回归。
- 面板 `truncated` 是**文件表窗口**的截断标记，与消息区「加载更早消息」的 `truncated` 语义同源但独立（各自档位）。
- 不可预览文件照列不预过滤（用户裁决）：列表可见性与可读性解耦，点击后才暴露 403。

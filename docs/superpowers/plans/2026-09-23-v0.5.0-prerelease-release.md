# 0.5.0-prerelease 发布流水线计划

> **执行方式：用 goal 执行**
> 日期：2026-09-23 · 分支：`feat/phase2-injection` · 主控窗口执行 + 每步 review 子代理验收
> 链路：feat/phase2-injection → PR 合入 main → 合并 PR #75/#77 → 再简化再验证 → CI → 发 0.5.0 prerelease + 图文 README

## 通用执行协议（适用于每一步）

1. 本计划文档即执行 SSOT，各步「验收标准」为 review 依据。
2. **每步完成 → 派 1 个 review 子代理**（加载 `code-review` 技能）对照该步「验收标准」检查产物，结论 = 达标 / 不达标 + 问题清单；不达标 → 主控修复 → **同一子代理复检**；达标 → 台账（`.superpowers/sdd/2026-09-18-phase2-injection-mainline/progress.md`）记一行后进入下一步。
3. 全程不 rebase 已推送历史；合并一律 merge commit（仓库惯例）；main 无分支保护。

## 探查结论（计划依据，2026-09-23 核实）

**能力矩阵（8 工具 = claude / codex / opencode / openclaw / kimi / workbuddy / zcode / dsh）**：本机通知 8/8；Skill 7 全 + dsh 只读启停；MCP 7 家（dsh 无）；插件 4 全 + kimi 仅文件型、workbuddy/zcode/dsh 无；Hook 仅 claude/codex/kimi；手机消息查看与文件预览 8/8（同一 get_all_sessions + content 派发链）；**手机发消息（注入）试验性 4 家：claude/codex/kimi/opencode**（`inject/routing.rs:35-43` 单点门控），openclaw/workbuddy 为 blackbox、zcode 为 M11 无头预留、dsh 另评。

**版本/发布机制**：三文件版本一致 0.4.2（从未打 tag 发布）；最新 release v0.4.1；`release.yml` 由 tag `v*` 触发双平台构建并建 draft release（正文读 `docs/release-notes/v<版本>.md`）+ updater latest.json；`ci.yml` 仅 PR/手动触发；合并惯例 merge commit。

**远端**：#75 移动端归档（MERGEABLE/UNSTABLE，CI 失败仅 `cargo fmt`（tunnel.rs，来自 fec59a7）+ `prettier` 6 文件）；#77 许可证合规（Draft/CLEAN）；#81/#82 已修未关，#79/#80 未修。

**本地**：领先 origin/main 225 提交；远端同名分支已推进到 `fec59a7`（本地是否包含待核）；工作区 3 个脏文件（question.rs / QuestionCard.tsx / 其测试）恰在 #80 修复区域；分支顶端已含批次 F（mcodex 权限菜单 picker + 锚点账本）等新提交。

---

## T0 · 分支拓扑核对与工作区收束

做法：fetch origin → 用 is-ancestor/merge-base 核实本地与 fec59a7、与 origin/main 的真实关系；缺 fec59a7 则 merge `origin/feat/phase2-injection`；若基于陈旧 main 分叉则 merge `origin/main` 一次收拢。检视 3 个脏文件：若为 #80 修复完成态 → 跑相关测试后单独提交（关 #80）；半成品则补完或 stash 留证。

**验收标准**：工作区零脏文件；fec59a7 归属明确（已并入或有记录理由）；cargo check + pnpm build 绿。

## T1 · code-simplifier 简化（第一轮，保守不改行为）

做法：按 code-simplifier 技能覆盖本分支累计改动的生产代码（`git diff --name-only <merge-base>..HEAD` 圈定，聚焦 inject/、remote/、mobile/，排除 tests/docs）；分 3 批派简化子代理，每批后 cargo check；顺带修复审遗留 kernel 文档注释 Minor；收敛为 1 个提交。

**验收标准**：无行为变更（后续 T2 门禁佐证）；无新增嵌套三元；改动仅可读性/一致性范畴。

## T2 · 功能验证（六门禁）

`cargo fmt --check` / `cargo clippy` / `cargo test --lib` / `cargo test --bins --features hook-listener`（CARGO_TARGET_DIR=target/gate-run）/ `pnpm vitest run` / `pnpm build + lint`，失败修绿。

**验收标准**：六门禁全绿，输出留证入台账。

## T3 · 新功能 HTML 原型图（仓库外侧）+ 截图入库

做法：在 `E:\LLMproject\Github\mam-mockups\`（worktree 外侧）画 4 张移动端 UI 布局：①会话详情-消息查看 ②发消息（composer+队列/插队状态条）③审批面板 ④多选题（含 Review 摘要）；Playwright 截 PNG 入库 `docs/images/mobile-{detail,send,approve,question}-v0.5.0.png`；HTML 不入库。

**验收标准**：4 张 PNG 入库、命名合规、画面覆盖对应功能要素（review 子代理看图核要素）。

## T4 · README / CHANGELOG / release notes 三件套

README.md（中文）：新增「多终端适配矩阵」章节（8×8 表 + 手机端说明：查看/预览 8/8、发消息试验性 4 家）；「可注入 4 终端现状与不足」小节（claude 排队整批取出/早 Esc 撤回窗口、codex Tab 进队列/Ctrl+C 撤回打断前回合、kimi 审批数字禁用走菜单、opencode Ctrl+C 禁用防退出等）；4 张图嵌入远程接入章节；修既有表 3 处过时项 + 终端跳转表补 Windows；早期版本寄语（较早期版本、中秋国庆体验、欢迎提 issue、作者假期远程响应）。README.en.md 补精简矩阵。CHANGELOG 新增 [0.5.0] 条目（覆盖 since 0.4.2 全量，含 #75/#77 内容按实际合入）。新建 `docs/release-notes/v0.5.0.md`（按 v0.4.1.md 格式，覆盖 since v0.4.1 含 0.4.2 未发布项，内嵌矩阵+截图+寄语）。

**验收标准**：矩阵与代码事实一致（review 子代理抽 routing.rs/adapter 抽查）；过时项清零；三件套相互不矛盾。

## T5 · 提交 PR + 单轮合并审查 + 直接合并

push 分支 → `gh pr create`（正文含矩阵+门禁证据）→ 派 1 个 review 子代理（规格+质量一轮，即本步验收）→ 阻塞则修复复审；可合并 → `gh pr merge --merge`（不等 CI）。

**验收标准**：审查结论零阻塞；PR 已以 merge commit 合入，main 含分支全部提交。

## T6 · 合并 PR #75 与 #77（阻塞即派子代理修）

#75：派子代理在临时 worktree 检出 `feat/mobile-archive-history` → merge `origin/main` 解冲突（保留双方功能）→ 修格式门（cargo fmt tunnel.rs + pnpm format 6 文件）→ 跑门禁 → push → merge。#77：`gh pr ready 77`，README/版本文件冲突同法解。顺序先 #75 后 #77。

**验收标准**：两 PR 均 merged；格式门已修；关键共享文件（api.rs/server.rs）无功能丢失（review 抽查）。

## T6.5 · 再简化 + 再验证（T7 前第二轮）

做法：对 T1 之后新入 main 的代码（#75 归档链改动 + 本窗口 T4–T6 增量）再跑一轮 code-simplifier（保守），随后六门禁全量重跑；改动经 merge commit 入 main（小 PR 或直推提交，遵循仓库惯例）。

**验收标准**：第二轮简化零行为变更；六门禁在合并后的 main 上全绿。

## T7 · main CI + 终版文档复核 + 0.5.0 prerelease 发布

`gh workflow run CI --ref main` → `gh run watch` 等绿（失败即修）。派子代理复核 README/CHANGELOG/release notes 已反映 #75（归档）与 #77（许可证）并更新到最新——缺则补提交。发版按 `docs/RELEASE.md` + `scripts/release-version.mjs`：版本 **0.5.0-pre.1**、tag **v0.5.0-pre.1**（不烧 0.5.0 正式号），notes 文件名对齐 workflow 读取逻辑；push tag → release.yml 构建 draft release → `gh release edit v0.5.0-pre.1 --draft=false --prerelease` 发布（双平台安装包 + latest.json 由流水线产出）。收尾：关 #81/#82（注明 4640651）；台账+验收清单补记；#79 留 open。

**验收标准**：main CI 绿；GitHub 上存在 prerelease 标记的 v0.5.0-pre.1 Release 且资产齐（Windows/macOS 安装包 + latest.json）；#81/#82 已关；文档复核子代理达标。

## 风险与预案

- T0 拓扑未知数：以 fetch 后证据定合并方向，不重写历史。
- #75 冲突面大（api.rs/server.rs/queue.rs 双方大改）：worktree 解冲突 + 全门禁后才 push。
- tag/notes 文件名机制：执行时核对 release.yml 源码与 RELEASE.md，以 workflow 实际解析为准。
- 不阻塞项：I-1 十项实机走查结果待用户回填（README 按「试验性」口径如实标注）；#79 留 open。
